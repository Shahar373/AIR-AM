# CHANGELOG

כל גרסה מתויגת ב-git (`vX.Y.Z`) ומוצגת בממשק (בכותרת). הפורמט לפי
[Keep a Changelog](https://keepachangelog.com/) ו-[Semantic Versioning](https://semver.org/):
**MAJOR** לשינוי שובר · **MINOR** לפיצ'ר · **PATCH** לתיקון.

מוסכמה: כל PR מעדכן את `VERSION` ומוסיף שורות תחת `[Unreleased]`; בעת מיזוג ל-main
מקדמים לכותרת גרסה ומתייגים את ה-commit.

## [2.17.0] - 2026-08-08

### נוסף
- **⚠ ‏CI על Python 3.11 **ו-3.13** (מטריצה) — הגרסה שרצה בייצור לא נבדקה כלל.**
  ה-CI נעץ 3.11 ("כמו Pi OS Bookworm") מאז ההתחלה, אבל ה-Pi שמריץ את הפרויקט
  כבר על **Debian 13 / Trixie** (‏`systemd 257`, אומת מהתקנה חיה) — כלומר
  Python 3.13. זהו בדיוק הפער שבו באג ספציפי-לגרסה עובר את כל ה-CI ומגיע ישר
  למשתמש. שתי הגרסאות בשטח (Bookworm ו-Trixie), ולכן שתיהן נתמכות ונבדקות.
  ‏`fail-fast: false` כדי שכשל באחת לא יסתיר את תוצאת השנייה. אומת: כל 508
  הבדיקות (ללא הדפדפן) עוברות גם על 3.13.
- **אינווריאנטות systemd כבדיקה (`tests/test_systemd_units.py`, 21 בדיקות)** —
  העיקרון המכריע בפרויקט ("‏SDR אחד, בהחלפה") נאכף כולו ע"י `Conflicts=`
  בקובצי היחידות ועל כך שאף צרכן אינו enabled — ושתי ההנחות נשמרו עד כה
  **בעין בלבד**. ‏`systemd-analyze verify` שכבר רץ ב-CI בודק תחביר, לא את
  ההיגיון. עכשיו נבדק: כל **זוג** מארבעת צרכני ה-SDR מודר הדדית (‏`Conflicts`
  סימטרי מרומז ⇒ נבדק לפי זוג, לא לפי כיוון), אף צרכן אינו נושא מקטע
  `[Install]`, ‏`sdrplay` ו-`airam-web` הם היחידים שעולים באתחול, כולם
  ‏`Requires`+`PartOf` על `sdrplay`, ‏`StartLimitBurst` קיים ל-satcom בלבד
  (בטיחות bias-T) ויושב ב-`[Unit]` ולא ב-`[Service]`.
  **הסיכון שזה סוגר:** ‏HFDL כמצב חמישי כבר בתור ההמשך; שורת `Conflicts`
  חסרה שם פירושה שני תהליכים על אותו מקלט — כשל שלא נראה כתקלה אלא
  כ"קליטה גרועה". אומת שהבדיקות נכשלות כשמסירים Conflicts או מוסיפים
  `[Install]`.
- **מטריצת ADS-C מלאה (`tests/test_adsc_matrix.py`)** — חילוץ המיקום מ-ADS-C
  נשבר **שלוש פעמים**, בכל פעם במסלול אחר, ושתי הפעמים הראשונות התגלו רק אחרי
  שמשתמש הצליב מול ADS-B חיצוני ומצא מיקום שגוי באלפי ק"מ. הבדיקות הקיימות
  כיסו מקרים *בודדים*; כאן נבדקת המטריצה כולה — 3 מסלולים (SATCOM · VDL2
  מסלול A · VDL2 מסלול B, שלכל אחד גייטינג משלו) × 3 כיוונים × 2 מצבי
  `decode_failed` — מול חוק אחד: **מיקום מותר ⟺ הפענוח הצליח וגם הכיוון אינו
  uplink**. אומת שהמטריצה מבודדת נכון: פגיעה ב-`_normalize_acars` מפילה את
  SATCOM ו-VDL2/A בלבד, ופגיעה ב-`_normalize_vdl2` מפילה את VDL2/B בלבד.
  נבדק גם שכישלון פענוח *נראה* למשתמש ("לא פוענח") ולא נבלע כ-`None`.
- **‏fuzzing של `_normalize_acars`** — 120,000 קלטים ממוטטים (מבוססי-seed
  מפורמטים אמיתיים + אקראיים) מול ארבע תכונות: לא זורק, נ"צ תמיד בטווח חוקי,
  אף פעם לא חצי-מיקום, ו-`notable` תמיד bool. **אפס ממצאים** — הפרסרים
  יציבים. מתועד כאן כדי שלא ייבדק שוב מאפס.
- **בדיקות התנהגות ל-UI בדפדפן אמיתי (`tests/test_ui_browser.py`, Playwright)** —
  השכבה השנייה מעל בדיקות התחביר/הסטטיקה. טוענות את `index.html` **כפי שהוא
  משודר** (בלי refactor, בלי build step — §7 נשמר), מזריקות תשובות API דרך
  route interception, ובודקות DOM אמיתי. שש בדיקות, שכולן מכסות באגים שקרו
  בפועל: הקפאת מונה ההודעות מעל 500, כישלון אימות שהכריז standby, צ'יפ
  הניתוק, והבחנה בין כשל רשת לכשל אתחול. בנוסף, ה-fixture מפיל כל בדיקה שבה
  נזרקה **חריגת JS לא-מטופלת** — צורת הכישלון שאין לה שום חיווי בשטח.
  - **הארכיון הרב-יומי ו-`createDataView`** — הקוד המורכב ביותר ב-UI, שעד כה
    לא נבדק כלל: מסע הלוך-חזור מלא (600 הודעות חיות ⇄ ארכיון) שמוודא
    ש-`exitArchive` משחזר את המונה **המצטבר** ולא גוזר אותו מחדש מחלון ה-500
    (אומת: בלי השחזור הבדיקה מראה 500 במקום 600); שהפולינג החי באמת נעצר
    בזמן עיון בארכיון גם אחרי מעבר בין תצוגות; ושלושת מופעי `createDataView`
    מבודדים זה מזה.
  - **‏job נפרד ב-CI** (‏`ui-browser`): החבילה המהירה ממשיכה לחסום מהר, ו-flake
    אפשרי של דפדפן לא חוסם עבודת backend. בלי playwright הקובץ מדלג בנקי.
  - **בלי המתנות מבוססות-sleep** — רק ה-auto-waiting של `expect()`. הפרויקט
    כבר נשרף מבדיקה תלוית-תזמון (ר' `no_sleep` למטה).
- **בדיקות frontend ראשונות בפרויקט (`tests/test_frontend.py`)** — עד כה ~3,000
  שורות ה-JS ב-`static/index.html` היו הצד היחיד בלי שום רשת ביטחון (ר'
  `docs/field-station-roadmap.md` §2.5: שבעה מהבאגים בשלוש הגרסאות האחרונות היו
  frontend-בלבד, וכולם התגלו בעין). ארבע בדיקות, בלי דפדפן/npm/build:
  - **שער תחביר דרך `node --check`** על הסקריפט ה-inline. אין build step ל-UI,
    ולכן שגיאת תחביר אחת משביתה את *כל* הממשק בלי שאף שלב תופס אותה קודם.
    ‏`node` כבר קיים ב-`ubuntu-latest`; חסר => הבדיקה מדלגת, לא נכשלת.
  - **בדיקה סטטית ש-poller רשת לא נשאר בלי `document.hidden`** (הרגרסיה למטה).
  - שמירה על השומר החסר ב-`tooOldForMap`, ועל קיום סקריפט inline יחיד.

### תוקן
**סבב הקשחה: עמידות בפני קלט פגום (`tests/test_corrupt_logs.py`, 12 בדיקות).
הזווית: כל הקוראים בפרויקט כבר סבלו *שורה חלקית*, אבל אף אחד לא סבל **קלט
תקין-מבנית אך שגוי-טיפוס** — והפער הזה הסתתר מאחורי `except ValueError`
שנראה כאילו הוא מכסה הכול:**

- **⚠⚠ הבאג החמור בסבב: שורה אחת פגומה ב-`acars.jsonl` מנעה מהתחנה כולה
  לעלות.** קובצי ה-JSONL נכתבים ב-append בלי fsync (במכוון — §12), והקוראים
  דילגו על שורה שנקטעה כי `json.loads` נכשל עליה. אבל שורה שהיא JSON **תקין**
  ואינה אובייקט (‏`null` / `0` / מחרוזת / מערך — שריד אפשרי של בלוק שהוקצה ולא
  נכתב, בדיוק תרחיש הכיבוי-הפתאומי שבשבילו נבנה `_atomic_write`) **עברה** את
  ה-`except ValueError`, והקוד ניגש מיד ל-`r.get("t")` ונפל ב-AttributeError.
  ‏`_load_acars_history()` / `_load_vdl2_history()` / `_load_satcom_history()`
  נקראות ב-`__main__` **בלי `try`** לפני `app.run()` ⇒ ‏`airam-web` לא עלה
  בכלל. וזה המתזמר שמשחזר את מצב ה-SDR באתחול (`_boot_restore`), כך שעם
  `Restart=always` נוצרה **לולאת קריסה שמשביתה את התחנה כולה** — ובשטח אין SSH
  כדי לאבחן. תוקן ב-`_jsonl_records`, נקודה משותפת אחת לכל ארבעת הקוראים
  (טעינת היסטוריה, ייצוא, ארכיון `?day=`).
- **`/api/activity` — אותה משפחת באג, 500 חוזר.** נקרא בפולינג כל 15 שניות
  במצב קול; שורה פגומה אחת ב-`activity.jsonl` הפילה אותו ב-TypeError בכל
  קריאה, לא פעם אחת.
- **`parse_stats` זרק `ValueError` לא-מטופל על קריאה קרועה.** התבנית
  `(-?[0-9.]+)` מקבלת גם `.`‏, `..`‏, `-.`‏, `1.2.3` — ו-`float()` עליהן נופל.
  הקובץ יושב ב-tmpfs ונכתב ~פעם בשנייה **בזמן שאנחנו קוראים אותו**, כך שקריאה
  קרועה היא תרחיש אמיתי. הנפילה לא הייתה מקומית: הפונקציה מזינה את
  `/api/metrics` (פולינג כל שנייה), את `/api/signal`, **ואת `_sample_probe_stats`
  — כלומר גם בדיקת האנטנה הייתה נכשלת**. עכשיו המדד הפגום מדולג והשאר נקרא.
  (‏`nan`/`inf` נבדקו ואינם מתקבלים בתבנית מלכתחילה — אין סכנת `NaN` ב-JSON.)
- **`adsb.process` זרק מנה שלמה בגלל רשומה פגומה אחת.** ‏`_loop` תופס ולא מפיל
  את ה-thread, אבל ה-poll כולו נזרק — דקה שלמה בלי מסלול פעיל ובלי אינדיקציית
  שיבוש, בגלל רשומה אחת ממקור חיצוני שלא מובטחת סכמתו. עכשיו הדילוג נקודתי.

**סבב ציד באגים על כל הפרויקט (backend + frontend + בדיקות), המשך ישיר לסבב
ה-4-סוכנים שלפניו — הפעם עם דגש על האזורים שהוא לא כיסה: תשתית הבדיקות עצמה,
תקציב ה-CPU של הניטור, ו-JS. שישה ממצאים, כולם עם אימות דו-כיווני (הבדיקה
הודגמה כנכשלת לפני התיקון ועוברת אחריו):**

- **⚠ `no_sleep` בבדיקות מיקף את `time.sleep` ה*גלובלי* — והפך בדיקה
  לתלוית-מזל.** ‏`monkeypatch.setattr(app.time, "sleep", ...)` נראה ממוקד
  ל-`app`, אבל `app.time` **הוא מודול `time` עצמו** (אומת: `app.time is time`
  → `True`), ולכן המיקוף חל על כל התהליך — כולל `time.sleep(0.2)` שבגוף
  הבדיקה עצמה, שאמור היה לתת ל-thread זמן לרוץ. הוא חזר מיידית,
  ו-`test_scan_loop_skips_failed_leg_and_recovers` עברה או נפלה לפי תזמון
  בלבד (עברה לבד, נפלה בריצה מלאה). **CI אדום אקראית שוחק את האמון בכל
  הבדיקות האחרות.** תוקן בשורש: `_scan_loop` ממתין עכשיו על
  `stop_evt.wait(step)` במקום `time.sleep(step)`, והבדיקה מסתנכרנת על
  `threading.Event` במקום על שעון קיר.
  - **רווח נלווה בייצור:** עצירת סריקה/מעבר מצב נקטעים **מיידית** במקום
    להשלים עד שנייה שלמה של שינה — זמן שהמשתמש ממתין לו בפועל.
  - **רווח נלווה ב-CI:** זמן ריצת החבילה ירד מ-~34ש' ל-~26ש' (‎-25%).
- **⚠ `/api/antenna/check` דגם בתנאים שונים מהבסיס => פסק-דין שקרי.**
  ‏"כבר במצב קול על אותו תדר" הספיק כדי לדלג על ההכנה המתוקננת (AGC, סקוולץ'
  פתוח). אבל רצפת הרעש ב-dBFS נמדדת *אחרי* שרשרת הרווח, כך שמשתמש שיושב בקול
  עם רווח ידני קיבל מדידה בסקאלה אחרת לגמרי מהבסיס שנמדד תחת AGC — ומכאן
  `verdict="below_baseline"`, שה-UI מציג כ"האנטנה כנראה מנותקת", **בלי ששום
  דבר בחומרה השתנה**. זו בדיוק ההמצאה שעקרון §12 אוסר, רק בכיוון של פסק-דין
  במקום ערך. תוקן: מדלגים על ההכנה רק כשהמצב החי זהה *בפועל* לתנאי הבדיקה
  (תדר + `am` + AGC + סקוולץ' פתוח).
- **⚠ הגבלת הקצב של ה-PIN לא הגבילה קצב.** הקוד השהה שנייה ואז בדק את ה-PIN
  בכל זאת. ‏Flask רץ `threaded=True` => ההשהיה מתרחשת **במקביל בכל thread**,
  ולכן תוקף עם 100 חיבורים בו-זמנית היה ממצה מרחב של 4 ספרות בכ-100 שניות —
  ולא ב-"~2.7 שעות" שההערה בקוד הבטיחה במפורש. תוקן: מעבר לסף, הבקשה **נדחית
  ב-429 בלי לבדוק PIN בכלל** עד סוף החלון — חסין למקביליות (5 ניסיונות לחלון,
  ולא משנה כמה חיבורים). החסימה מוגבלת-בזמן ומתפוגגת לבד => DHCP/NAT לא
  נענשים לצמיתות, כפי שהיה התכנון המקורי. **מחיר מכוון:** גם PIN נכון נדחה עד
  סוף החלון — אחרת תוקף מקבילי היה ממשיך לנחש בחינם.
- **`_pin_fails` גדל ללא הגבלה** — נמחק רק בהצלחה, כך ש-IP שנכשל ולא הצליח אף
  פעם נשאר בזיכרון עד אתחול. תוקן: `_pin_prune` גוזם חלונות שפג תוקפם.
- **`/api/health` פתח שישה תהליכים בכל קריאה** (`systemctl is-active` ליחידה),
  בפולינג של 10 שניות מכל טלפון פתוח => ~2,160 forks בשעה. תוקן: `systemctl
  is-active` מקבל את כל היחידות ב-fork **אחד** ומחזיר שורה לכל אחת (פי 6
  פחות). בפרויקט שמוותר על מחצית מדמודולטורי ה-SATCOM (`skip_c`) כדי לשרוד על
  ספק כוח שולי, ניטור עצמי לא אמור להיות הצרכן הגדול בתקציב.
- **`pollGlobalState` היה ה-poller היחיד בלי `document.hidden`** — ‏PWA שנשארת
  פתוחה ברקע בכיס (בדיוק תרחיש השטח) המשיכה לתשאל את ה-Pi לנצח, וכל קריאה
  שלה היא `/api/health`. תוקן + נוספה בדיקה סטטית שתתפוס כל poller עתידי.
- **`tooOldForMap` לא בדק `lastT` חסר, בניגוד לתאום שלו `isStale`** — ב-JS
  `null * 1000` הוא `0` (=1970), ולכן מטוס שהודעותיו הגיעו בלי חותמת זמן
  מספרית (מקרה ש-`updateCraft` מגן עליו במפורש) נחשב מיד "ישן מדי" והסמן שלו
  נמחק מהמפה בפריים הבא אחרי שנוצר.
- **סמן על המפה נשאר עם התווית הגרועה ביותר לכל הסשן** — `upsertMarker` עדכן
  מיקום ו-popup לסמן קיים אבל לא את ה-tooltip, בעוד שהזהות מתחדדת תוך כדי
  (icao → מספר טיסה → רישום).
- **`c.ebno.toFixed(1)` בפאנל כיוון האנטנה בלי בדיקת טיפוס** — ערוץ שהגיע בלי
  `ebno` מספרי זרק `TypeError` שנתפס ב-`catch` החיצוני והוצג כ**"אין תשובה
  מה-Pi"** — הודעה מטעה לגמרי כשה-Pi ענה מצוין. תוקן ב-`aimNoDemod` (מוצג "—").
- **הערה שגויה ב-`adsb.py`**: `events` תועד כ-3-tuple בעוד הקוד כותב וקורא
  4-tuple (`mode` נוסף). בפרויקט שבו ההערות הן מקור-אמת, זו לא קוסמטיקה.
- **⚠ כישלון אימות גרם ל-UI להכריז "המקלט בכיבוי" בזמן שהוא משדר.** תשובת
  `_guard` (‏401 בלי PIN, או 429 החדש) נעצרת *לפני* לוגיקת המצבים ולכן **אין
  בה `state`** — ו-`applyMode` נפל ל-`back = (data.state && ...) || "off"`,
  כלומר הסיק "standby" מהיעדר שדה. ה-SDR לא נגע כלל. באג קיים (לא נולד
  מהשינוי ל-429): גם ביטול תיבת ה-PIN הפעיל אותו. תוקן ביציאה מוקדמת על
  `data.auth` שגם **מבטלת את העדכון האופטימי** ומחזירה את המשתמש לתצוגה
  שממנה לחץ — סינכרונית, בלי להישען על הפולינג התקופתי.
  - ⚠ **הניסיון הראשון לתקן היה שגוי בעצמו:** הוא קרא ל-`pollGlobalState()`
    מתוך `applyMode`, אבל הפונקציה הזו יוצאת מיד כש-`applyModeInFlight` דלוק
    (השורה הראשונה שלה) — כלומר הסנכרון היה **no-op שקט**. התגלה רק כשנכתבה
    בדיקת דפדפן שניסתה לאמת אותו. הוחלף בשחזור סינכרוני, שניתן לבדיקה.
- **⚠ *כל* שגיאת אתחול הוצגה כ"אין חיבור לשרת".** ה-`.catch` של שרשרת האתחול
  לא הבחין בין כשל ה-`fetch` עצמו לבין חריגה שנזרקה **בגוף** ה-`then` — כך
  ששרת שענה מצוין, עם שדה חסר בתשובה (אי-התאמת גרסה אחרי שדרוג, תשובה
  חלקית), שלח את המשתמש לבדוק Wi-Fi ו-USB בשטח. תוקן: מסלול דחייה נפרד
  ל-fetch (ארגומנט שני ל-`then`) מול `.catch` לשגיאת אתחול, עם הודעות שונות.
  אותו עיקרון §12 — לא מדווחים תקלה שלא זיהינו באמת.
- **`presetFor` הפיל את כל האתחול על שדה חסר** — `cfg.presets.find(...)` בלי
  הגנה, ודרך `updateDisplay` זה הפיל את שרשרת האתחול כולה והשאיר את הדף על
  שלד הטעינה. פריסטים הם קישוט; הם לא צריכים להיות תנאי לעליית הממשק.

**סבב ניתוח-קוד מקיף (4 סוכנים במקביל: concurrency/state, פרסרים, ops/security, UI) — ~45 ממצאים, כולם עם regression test. סבב בדיקה עצמאי נוסף (סוכן חמישי, בלי הקשר מהתיקונים) על ה-diff המלא לפני מיזוג מצא 2 רגרסיות קטנות נוספות — שתיהן תוקנו:**

- **`_LOADSHEET_UNIT`: `\b` מחוץ לקבוצת היחידה הפיל התאמות שלמות** — מספר
  מוצמד-בלי-רווח לסיומת לא-מוכרת ("62000KGS"/"62000K") איבד את כל ה-ZFW/TOW/TOF,
  בעוד הקוד המקורי (בלי מושג יחידה) כן היה תופס את המספר. תוקן: ה-`\b` עבר
  לתוך קבוצת היחידה (מותנה בה), כך שסיומת לא-מוכרת מדירה רק את היחידה, לא את המספר.
- **הענף המילולי של `_ATIS_WIND_RE` (`WIND ddd/ss`) המציא "KT" כשלא הייתה
  בטקסט כלל** (`m.group("u3") or "KT"`) — הפרה ישירה של אותו כלל §12 שהתיקון
  הזה עצמו אוכף ב-loadsheet/nav_fuel. תוקן: בלי KT בקלט = בלי KT בפלט.

- **⚠ `_voice_tune` מחק בשקט satcom_bias_tee/satcom_gain/signal_baseline/scan_plan
  בכל כיוונון קול.** בנה state חדש מ-`{**params,...}` במקום למזג על גבי ה-state
  הקודם — כניסה הבאה ל-SATCOM הייתה מדליקה מחדש bias-T (`load_state` ממזג
  מ-`DEFAULT_STATE=True`) גם כשהמשתמש כבר מזין LNA חיצוני. תוקן: מיזוג על גבי
  ה-state הקודם, לא דריסה.
- **scan "זומבי" בשלושה תרחישים נפרדים:** `/api/tune` לא עצר סבב סריקה פעיל
  (רק `/api/mode {mode:voice}`); `_boot_restore` יכול היה להריץ שני scan
  threads במקביל כשהמשתמש כבר התחיל את אותו לוח בזמן ההמתנה ל-SDR (ה-thread
  הישן נשאר בלתי-ניתן-לעצירה); `/api/mode` למצב voice עצר את הסבב *לפני*
  נעילת TUNE_LOCK, בניגוד לכל שאר המצבים (בקשה שנכשלת ב-409 הייתה עוצרת סבב
  תקין בחינם). תוקן ושלושתם מכוסים ב-regression tests.
- **`/api/antenna/check` תוך כדי סבב סריקה שיחזר את הבנק הכללי במקום את הרגל
  הפעילה בפועל** — רגל עם `freqs` מפורשים (שלא נכתבים ל-`state["acars_freqs"]`)
  הייתה נשארת תקועה על בנק שגוי אחרי בדיקת אנטנה. תוקן: `_restore_after_probe`
  משחזר את הרגל הפעילה מ-`_scan_status` כשסבב פעיל.
- **`_enter_standby` בתוך `_scan_loop` רץ בלי TUNE_LOCK** בשני מקומות (סבב-בלי-רגל-בחלון,
  כל-הרגלים-נכשלו) — יכול היה לרוץ בו-זמנית עם `/api/antenna/check`. תוקן:
  עטוף ב-TUNE_LOCK, best-effort אם תפוס.
- **`/api/session/ack` כתב ל-state.json בלי שום נעילה** — יכול היה לקרוא state
  ישן ולדרוס בחזרה app_mode חדש (lost update) אם הגיע בדיוק תוך כדי מעבר מצב.
  תוקן: תחת TUNE_LOCK כמו כל שינוי אחר (409 כשעסוקה, לא תקיעה/דריסה שקטה).
- **`off` פעמיים ברצף (טאב כפול) דרס `prev_mode` ל-"off" עצמו** — כפתור
  ההדלקה היה מאבד את המצב האמיתי האחרון. תוקן: לא דורסים prev_mode כשכבר off.
- **`_journal_tail` בלי timeout** — journalctl תקוע (journald לא מגיב) בזמן
  שהקורא מחזיק TUNE_LOCK היה תוקע את כל שינויי המצב העתידיים לנצח. נוסף timeout=5s.
- **`.txt` יתום (הקלטה שכבר נמחקה) לא נוקה לעולם** מ-`_sweep_recordings` —
  צבירה איטית לאורך זמן. תוקן.
- **VDL2 מסלול A (ACARS-over-AVLC) לא העביר `_structural_dir`** ל-`_normalize_acars` —
  חסם ה-ADS-C-uplink שתועד ותוקן *פעמיים* ב-v2.15.1/v2.15.2 (ר' למעלה) פשוט
  לא היה קיים במסלול הזה, דווקא הכי סביר לשאת ADS-C אמיתי. אותו קלאס-באג
  בדיוק, מסלול שלישי.
- **`_UPLINK_HEADER_RE` תפס גם רישומי מטוס אמיתיים, לא רק כתובות תחנת-קרקע** —
  acarsdec מרפד רישומים קצרים בנקודה מובילה (`.N806NN`) בדיוק כמו כתובות
  תחנה (`.TCARC`); רישומי US/JP/KR איבדו את מיקומם. 4X-/C-/G- לא נפגעו (המקף
  שובר את התבנית) — לכן לא נתפס בקליטה מקומית. תוקן: ספרה בתג פוסלת סיווג-תחנה.
- **VDL2 מסלול B: מיקום ADS-C לא היה exclusive מול CPDLC** — הודעת CPDLC
  שמזכירה "ADSC" בטקסט חופשי מקונן עדיין ייחסה waypoint כמיקום מטוס, גם
  כש-category כבר הוכרע כ-"CPDLC". תוקן: גייטינג לפי category, לא is_adsc גולמי.
- **`_scan_latlon` הרכיב קואורדינטה מ-lat ו-lon משני תת-עצים לא-קשורים** —
  לקח lat ראשון ו-lon ראשון בעץ בנפרד. תוקן: זוג חייב להיות מאותו dict;
  "basic_report" מועדף על פני waypoints עתידיים (predicted_route/next_wpt).
- **רוסטר מאוחד: מטוס אחד מופיע כשתי שורות** כש-VDL2 מערבב פריימי ACARS-over-AVLC
  (עם reg) ופריימי XID/x25 (בלי reg, רק icao) מאותה כתובת AVLC. תוקן: מיזוג
  post-pass של buckets icao/flight לתוך bucket עם reg תואם.
- **תחנות קרקע (SQ squitter) הופיעו ברוסטר כ"מטוס"** בשם כתובת התחנה. תוקן:
  `tail_is_station` מוזרם מ-`_normalize_acars` ומדיר את ה-tail מהזהות.
- **`_parse_wx_alternates` זיהה מילות METAR נפוצות (TEMP/DEWP/INFO/GATE/...)
  כשדה-נחיתה חלופי.** הורחב ה-blocklist.
- **`_parse_loadsheet`/`_parse_nav_fuel` המציאו יחידות (kg/t) שלא היו בטקסט**
  כלל, וקטעו משקלים עשרוניים (`\d+` בלבד). תוקן: יחידה רק כשהיא בפועל בטקסט
  (kg/lb/lbs), תמיכה בעשרוני.
- **`_parse_atis` לא זיהה קבוצת רוח בתקן METAR הרגיל** (dddssKT מחובר, בלי
  "/") — הפורמט הנפוץ ביותר בפועל; וגם איבד gust/יחידה בענף המילולי. תוקן.
- **VDL2: `msg_num`/`msg_num_seq` כ-int הפילו את כל הפריים** (`TypeError` בחיבור
  מחרוזות). תוקן עם `str()`.
- **DNS-rebinding: `_guard` לא מגן מפניו בפועל** (Origin==Host גם תואמים
  בהתקפת rebinding) — התיעוד ב-§9 טען הגנה שלא הייתה. **החלטה מכוונת לא
  לצמצם** גישה (Host allowlist היה שובר "כל מכשיר, כל hostname, DHCP") —
  התיעוד תוקן לדייק, לא הקוד.
- **install.sh: כשל בנייה (cmake/make) לא עצר את הסקריפט** — `set -e` לא
  תופס כשל של פקודה לא-אחרונה ברשימת `&&` (אומת אמפירית), אז חתימת-בנייה
  נכתבה גם אחרי כשל שקט; שדרוג עתידי היה נעול על בינארי ישן/פגום לנצח. נוסף
  `|| die` מפורש בכל שרשרת בנייה (SoapySDR/rtl_airband/libacars/acarsdec/
  dumpvdl2/inmarsat-sniffer) + אימות-לאחר-בנייה ל-SoapySDR/libacars.
- **install.sh: `LIB=$(ls ... | head -1)` הפיל את כל ההתקנה בשקט** (בלי
  הודעת שגיאה) כש-SDRplay משנה מוסכמת-שמות — `pipefail` מפיץ את כשל ה-`ls`
  גם כש-`head` מצליח (אומת אמפירית). נוסף `|| true`.
- **sudoers נכתב ישירות ליעד ורק *אחר-כך* אומת** — קובץ פגום שנשאר שם אחרי
  כשל ולידציה היה מנעל `sudo` למערכת כולה. תוקן: כתיבה לקובץ זמני, אימות,
  ואז `install -m440`.
- **`/etc/airam/airam.env` (מכיל AIRAM_PIN) נוצר 0644 — world-readable.**
  נוסף `chmod 640`.
- **PIN: השוואת `==` (לא בזמן-קבוע) ובלי rate-limit** — מרחב 4-ספרות (10,000)
  ניתן-למיצוי תוך שניות. תוקן: `hmac.compare_digest` + השהיה רכה (לא חסימת-IP
  קבועה) אחרי כמה כשלונות רצופים.
- **ייצוא CSV חשוף להזרקת נוסחה** — `text`/`tail`/`decoded` מגיעים משידור
  רדיו, ותא שמתחיל ב-`=`/`+`/`-`/`@` מתפרש כנוסחה חיה ב-Excel/Sheets. נוסף
  `_csv_safe` (`'` מוביל).
- **satcom readiness gate (`airam-wait-sdrplay`) יכול לצרוך את תקציב ה-restart
  הסופי של airam-satcom** — "בעיטת" restart ל-sdrplay.service אחרי 8 ניסיונות
  מפיצה חזרה ל-airam-satcom עצמו דרך `PartOf=` אם הוא הקורא. תוקן: `%n` מועבר
  לשער, ומדלג על הבעיטה כש-airam-satcom הוא הקורא.
- **`airam-web.service`: `StartLimitIntervalSec=0` היה תחת `[Service]`**
  (תקף רק לפני systemd 230) — systemd מודרני מתעלם ממנו בשקט, כך שמשטח-הבקרה
  היחיד היה כפוף לתקרת ברירת-המחדל במקום "מתאושש לנצח". נתפס ותוקן דרך בדיקת
  `systemd-analyze verify` החדשה ב-CI. הועבר ל-`[Unit]`.
- **UI — VDL2: הודעות לא-ACARS (XID/CPDLC/ADS-C) מאותו מטוס התמזגו לכרטיס
  אחד בפיד** — `dedupKey` התנוון ל-`"<icao>||"` זהה לכולן (tail=label=text=null
  במסלול B). תוקן: category/decoded מבדילים גם כש-text ריק.
- **UI — SATCOM: בקרות bias-T/skip-C/gain נשארו פעילות בזמן שהמצב כבר רץ**
  בפועל, בלי שום מסלול קוד ששולח אותן מחדש — מצג-שווא ששינוי משפיע על
  החומרה. תוקן: ננעלות (disabled) כש-SATCOM פעיל, עם רמז "עצור כדי לשנות".
- **UI — ארכיון: פתיחת מצב חי בזמן צפייה בארכיון הזריקה הודעות חיות לתוך
  תצוגת הארכיון**, וחזרה מארכיון לא שחזרה `craft`/`icaoToId` (רק `msgs`,
  חלון-500) — מונים (מטוסים/⬆/⬇) נחתכו ל-500 ההודעות האחרונות בסשן ארוך.
  תוקן: `poll()` נחסם בזמן ארכיון, `craft`/`icaoToId` נשמרים ומשוחזרים במפורש.
- **UI — XSS: `renderSession` הזריק `decoded`/`tail` (טקסט חופשי משידור רדיו,
  עד 300 תווים) ל-`innerHTML` בלי escaping.** תוקן: בנייה דרך `textContent`
  (אומת בדפדפן אמיתי — payload עם `onerror` לא רץ יותר).
- **UI — "אין קשר לשרת" שווא כשחוזרים לטאב/PWA** אחרי יותר מ-12ש' ברקע —
  ה-interval שבודק ניתוק ממשיך לרוץ ברקע גם כשכל הפולינג עצמו מושהה. תוקן:
  מרענן את שעון-הקשר האחרון ב-`visibilitychange`.
- **UI — בורר תאריך הארכיון חישב "מקסימום" לפי UTC (`toISOString`) במקום שעון
  מקומי** — בשעות הקטנות (למשל 01:30 ב-UTC+3) חסם את בחירת "היום" האמיתי,
  בדיוק כשארכיון-לילה הכי רלוונטי. תוקן.
- **UI — `reloadStream` (MediaSession, לחצן ▶ במסך-נעילה) לא בדק satcom.active** —
  לחיצה תוך כדי SATCOM ניסתה להתחבר לזרם Icecast מת. תוקן.
- **UI — מפה: markers/tracks לא נמחקו לעולם, רק `removeLayer`** — סשן
  headless ארוך צובר עותק (כולל popup DOM) לכל מטוס שנראה-אי-פעם. תוקן.
- **UI — תא "🛰 ADS-B" בסטטיסטיקת SATCOM תמיד הציג 0** — `/api/satcom` לעולם
  לא מגיש שדה `adsb` (המפענח לא חושף אותו). הוסר התא (לא רק "יפה" — היה
  מטעה).
- **UI — עורך לוח הסריקה: `<select>` מצב-רגל בלי `<option>` תואם ל-`leg.mode`
  לא-מוכר** היה מציג "📻 קול" שקרי (latent — satcom עדיין לא scannable).
  תוקן.
- **UI/שרת — ספירה-לאחור לרגל הבאה בסריקה השוותה שעון-שרת מול שעון-לקוח
  בלי סטייה** — Pi headless בלי RTC/NTP יכול היה להציג ספירה שגויה. נוסף
  שדה `now` ל-`GET /api/scan`, ומחושבת סטייה חד-פעמית בלקוח.

### נוסף
- **`_mode_reconcile_loop`** (thread רקע, כל 60ש'): מתאושש מקריסת
  `sdrplay.service` שההפעלה-מחדש הפנימית שלה (לא restart job חיצוני) לא
  בהכרח מופצת דרך `PartOf=` לצרכן שהיה פעיל — סוגר פער אמיתי במטרה #3
  ("מתאוששים מקריסה לבד"). לא נוגע ב-off/scan/satcom (סיבות משלהן, ר' §12).
- **`shellcheck` + `systemd-analyze verify`** נוספו ל-CI — תפסו בפועל את באג
  ה-`StartLimitIntervalSec` למעלה תוך כדי הכנת התיקון הזה.

### בדיקות
- כ-40 בדיקות regression חדשות/מעודכנות פרושות על tests/test_app.py,
  test_scan.py, test_signal.py, test_session.py, test_boot.py, test_acars.py,
  test_vdl2.py, test_roster.py, test_security.py — אחת לפחות לכל ממצא למעלה.

## [2.15.3] - 2026-08-04
### תוקן
- **⚠ מיקום ADS-C עדיין יכול להיות שגוי גם בלי שגיאת פענוח גלויה — תיקון
  שני לאותו נושא.** אחרי התיקון הקודם (חוסם מיקום כש-`decode_failed=True`),
  משתמש הצליב את "הוכחת ההצלחה" המקורית (A7-BBB, 18.34°N/2.11°E) מול ADS-B
  חיצוני וגילה **שגם היא הייתה שגויה** — לא נתפסה קודם כי לא הייתה שם שום
  שגיאה גלויה (`decode_failed=False`). שורש הבעיה, מאומת ישירות מ-`libacars/
  adsc.c` במקור: ADS-C משתמש ב-tag מספרי (7 ואחרים) ש**פירושו הפוך לגמרי**
  לפי כיוון ההודעה — `la_adsc_uplink_tag_descriptor_table` (tag 7 = "Periodic
  contract request", קרקע→מטוס, *אין בו מיקום מטוס בכלל*) מול
  `la_adsc_downlink_tag_descriptor_table` (tag 7 = "Basic report", מטוס→קרקע,
  מיקום אמיתי). אם ההודעה מפוענחת עם טבלת הכיוון הלא-נכונה, הפענוח "מצליח"
  מבנית — בלי שום `err` שמתגלה — אבל שולף בייטים שהם בכלל פרמטרי-בקשה כאילו
  הם נ"צ. **גם A7-BBB וגם C-GHKX (ר' התיקון הקודם) התבררו כ-`dir="uplink"`**
  — כלומר "הוכחת ההצלחה" המקורית הייתה בעצמה אותו באג. תוקן: `_structural_dir`
  (SATCOM, מחושב עכשיו *לפני* הקריאה ל-`_normalize_acars`) ו-`direction`
  (VDL2, כבר זמין מוקדם) חוסמים מיקום ADS-C כש-uplink, בלי תלות ב-
  `decode_failed`. ⚠ **המשמעות: אין לנו עדיין ולו הוכחה אחת ש-ADS-C downlink
  אמיתי עובד** — כל קליטה עד כה הייתה 100% uplink (ר' התיעוד הקודם על
  "P channel בלבד"). 6 בדיקות regression חדשות/מעודכנות (SATCOM + VDL2 +
  הליבה המשותפת), כולל מקרה "positive" מתוקן שמראה שהוא בעצמו היה שגוי.

## [2.15.2] - 2026-08-04
### תוקן
- **⚠ מיקום שגוי מ-ADS-C גם כשהמפענח עצמו דיווח שהפענוח נכשל.** דווח ע"י
  משתמש (הצליב מול ADS-B חיצוני): הודעת A6 אמיתית (C-GHKX) הציגה מיקום
  5.69°N/2.11°E — אלפי ק"מ מהמיקום האמיתי (~45.85°N/‑29.6°W) — על אף
  ש-`decoded` שלה עצמו כבר אמר "לא פוענח". הסיבה: תיקון קודם באותו PR (ר'
  [Unreleased] הקודם) הוסיף זיהוי `decode_failed` (המפענח דיווח `err:true`
  על היישום המקונן), אבל **חילוץ המיקום (`_scan_latlon`) המשיך לרוץ ללא תלות
  בדגל הזה** — מבנה שהמפענח סימן ככושל יכול עדיין להכיל שדה `lat`/`lon`
  שרירותי (שריד-מפענוח-חלקי), וה-scan הרקורסיבי לא הבחין בין זה למיקום אמיתי.
  תוקן ב-2 המקומות (SATCOM וגם VDL2 מסלול B — אותה משפחת באג): מיקום
  מ-ADS-C מותנה עכשיו ב-`not decode_failed`, וכך גם `group="position"`
  (אחרת כרטיס-שנכשל היה מסונן תחת "📍 מיקום" ב-UI בלי מיקום אמיתי). ⚠ מסקנה
  מתודולוגית: ההודעה הקודמת ("ADS-C מאומת בקליטת שדה") התבססה על הודעה אחת
  שהצליחה, בלי השוואה מול מקור-אמת חיצוני — לא הייתה הוכחה מספקת. 3 בדיקות
  regression חדשות (SATCOM + VDL2 + הליבה המשותפת).

## [2.15.1] - 2026-08-04
### תוקן
- **⚠ "מעל 500 הודעות המערכת לא רושמת יותר" — היה נכון ויזואלית, לא בפועל.**
  הפיד/הרוסטר/הייצוא המשיכו לעבוד נכון מעל 500 הודעות (אומת בסימולציה ישירה
  מול השרת — ה-ring buffer + cursor ממשיכים לעבוד תקין). הבאג היה בסטטיסטיקת
  ה-UI בלבד: `renderStats()` חישב `StTotal`/`StCraft`/`StPos`/`StUp`/`StDown`
  מתוך `msgs` — מערך חלון-נגלל בגודל `MAX`=500 שקיים *רק* עבור הפיד/מפה/חיפוש
  (הכי-ישן מוחלף בהכי-חדש ברגע שעוברים 500). ברגע שסשן חצה 500 הודעות, כל
  המספרים האלה "קפאו" על הערך האחרון שלהם — נראה בדיוק כמו "הפסיק לרשום".
  תוקן: `totalMsgs` — מונה חדש שרק גדל (מתאפס עם `lastId` בזיהוי restart של
  השרת בלבד) — עבור `StTotal`; `StCraft`/`StPos` נקראים עכשיו מ-`craft`
  (שכבר לא נגזם — היה תמיד המקור הנכון, פשוט לא נעשה בו שימוש); `StUp`/
  `StDown` מסוכמים מ-`craft[id].up/down` (שנצברו נכון תמיד, רק לא נקראו
  נכון). `StRate` נשאר מבוסס-`msgs` בכוונה (מגמה עדכנית, לא ממוצע-כל-הסשן).
  ארכיון-יום משמר/משחזר את `totalMsgs` יחד עם `lastId`/`feedMax`. משותף
  ל-ACARS/VDL2/SATCOM (`createDataView`), לא SATCOM-ספציפי.

## [2.15.0] - 2026-08-04
### נוסף
- **פענוח ADS-C/CPDLC: אימות ראשון מקליטת שדה אמיתית (לא רק הנחה מהמקור).**
  לכידת `inmarsat-sniffer --feed -v` גולמית אחת (עם אישור מפורש מהמשתמש) אישרה:
  (1) **ADS-C עובד בפועל** — הודעת A6 אמיתית עם `arinc622.adsc.basic_report.
  {lat,lon}` הניבה `pos_src="adsc"` עם נ"צ אמיתי על המפה, בדיוק כפי שהמנגנון
  תוכנן. (2) **CPDLC (14 הודעות בקליטה) נכשל לרוב לא בגלל הקוד שלנו** — המפענח
  עצמו (`inmarsat-sniffer`/`libacars`) מחזיר `"err":true` על היישום המקונן (למרות
  ש-CRC המעטפת החיצונית תקין) — כנראה איתות שולי מדי לתוכן ברמת נעילה ראשונית,
  לא חוסר-תמיכה בקוד. `_libacars_decode` (משותף SATCOM/VDL2) מבחין עכשיו במפורש
  בין "לא ניסינו לפענח" ל"ניסינו, והמפענח נכשל" — `decoded` מציג הודעה מפורשת
  ("לא פוענח — המפענח החזיר שגיאה") במקום `None` זהה לשני המקרים. §12: לא
  ממציאים טקסט-תוכן, אבל כן חושפים עובדה אמיתית שקיימת במבנה. פענוח CPDLC-לטקסט
  קריא (clearance/instruction בפועל) עדיין לא מומש — דורש דגימה עם `err:false`
  שעדיין לא נצפתה בשדה, כדי לא לנחש שמות שדות.

### תוקן
- **⚠ מיקום שקרי על המפה: הודעת H1 עם תוכנית טיסה יכלה לקבל 📍 שגוי.** התגלה
  מקליטת שטח אמיתית (SATCOM, 33 דק'/465 הודעות/84 מטוסים) — נ"צ שקרי יחיד
  בכל הקובץ, מתחנת-קרקע (`tail=".TCARC"`) שהודעת תוכנית-הטיסה שלה
  (`#M3FPN/.../F:IVAKI,N32558E015065..LUMED,...`, שרשרת waypoints) קיבלה את
  נ"צ ה-waypoint הראשון כאילו הוא מיקום ה"מטוס" בפועל. שני תיקונים עצמאיים
  (כל אחד תופס מה שהשני מפספס): **(1)** `_text_latlon` דורש כעת בדיוק
  התאמת-קואורדינטה *אחת* — 2+ = שרשרת/מסלול, לא דיווח מיקום בודד, מוחזר
  `None` במקום לנחש איזו התאמה נכונה. **(2)** tail דמוי-כתובת-תחנת-קרקע
  (`_UPLINK_HEADER_RE`, אותו דפוס שכבר משמש לזיהוי הדר-ניתוב בטקסט) לא מקבל
  מיקום מנתיבי ה-heuristic (`_text_latlon`/`_parse_label16`) בכלל — תחנת-קרקע
  לא טסה. אותה משפחת באג כמו "לקח 1.7.1" ב-`_parse_sq` (כתובת תחנה ≠ מיקום
  מטוס), רק דרך מנגנון שונה. 6 בדיקות regression חדשות, כולל אחת מקצה-לקצה
  דרך `_normalize_satcom`.
- **`_H1_SUB_RE`/`_parse_fpn` לא תפסו אף הודעת H1 אחת בקליטת שדה אמיתית
  (0/12).** הפורמט האמיתי מ-`inmarsat-sniffer` הוא `"- #XX..."` (מקף+רווח
  לפני ה-`#`, לא `#` ממש בתחילת הטקסט — גם `.HELASAY...\n- #DFREQ02` עם ה-`#`
  אחרי `\n`) ו-`"M3FPN/"` (סאב-לייבל מודבק ישירות ל-FPN, בלי קו-נטוי פותח,
  בניגוד ל-VHF `"/FPN/"`). שתי הרחבות **תוספתיות-בלבד** — הפורמט המקורי
  (VHF, `"#XX..."` בתחילת הטקסט) נשאר עובד ללא שינוי, מכוסה ע"י הבדיקות
  הקיימות. תוצאה: הודעות H1/FPN אמיתיות מ-SATCOM מקבלות עכשיו `decoded`
  קריא (למשל "FMC 3 · תוכנית טיסה HLMS→LIEO · IVAKI LUMED SENTI...") במקום
  כלום.

### נוסף
- **📊 פירוט ⬆/⬇ (uplink/downlink) בסטטיסטיקת SATCOM.** נוסף מקליטת שטח
  אמיתית ראשונה שהצליחה (16 דק', 206 הודעות, 54 מטוסים שונים): **100% מההודעות
  היו uplink** (קרקע→מטוס) — אפס downlink, אפס lat/lon מפוענח. זו לא תקלה
  אלא פיזיקה צפויה של Inmarsat Classic Aero: ה-P channel (קרקע→מטוס) הוא
  שידור-שידור גלובלי חזק מה-GES שנועל ראשון וקל; ה-R/C channels (מטוס→קרקע)
  חלשים משמעותית ותלויי-כיוון אנטנת המטוס. בלי הפירוט הזה, "P channel בלבד"
  ו"קליטה מלאה דו-כיוונית" נראים זהים בפיד. שתי תוספות סטטיסטיקה חדשות
  (`satcomStUp`/`satcomStDown`) ב-`.acars-stats`, SATCOM-only (guard לפי
  קיום ה-DOM ב-`renderStats`, לא קוד מיוחד בפקטורי `createDataView`).
- **4 labels שנצפו בקליטת השטח האמיתית נוספו ל-`ACARS_LABELS`:** `A0`
  (AFN · רישום רשת — מכיל "AFN" בטקסט עצמו, ARINC 620), `1B`/`4P`/`2F`
  (לא לייבלים אוניברסליים מתועדים — מיפוי מבוסס על תוכן שנצפה בפועל, כמו
  התקדים הקיים ל-`16`/`1L`). קודם נפלו ל-fallback הגנרי "Label X".
- **🎚 שליטה ברווח SATCOM: AGC או gRdB ידני.** עד עכשיו מצב SATCOM רץ תמיד
  ב-AGC של הדרייבר בלי דרך לשנות זאת. נוסף מתג "AGC אוטומטי" + סליידר רווח
  ידני בתצוגת SATCOM, ופרמטר `gain` ל-`POST /api/mode` (‏`null`=AGC,
  מספר=gRdB 20–59, נחתך כמו בכלי עצמו). הבחירה נשמרת ונזכרת ב-state כמו
  `bias_tee`/`skip_c`/`satcom_freqs`. **למה זה משנה כשאין נעילה:** ה-AGC
  מכוון ל-setpoint של ‎-30dBfs לפי **כל** האנרגיה בחלון, כך ששידור סלולרי
  חזק סמוך ל-L-band עלול לגרום לו להוריד רווח ולהחניק את נשא הלוויין החלש;
  ברווח ידני הכלי גם מקבע `LNAstate=0` (רווח RF מקסימלי) — בדיוק מה שרוצים
  לאות לוויין. ⚠ בניגוד לתצוגת הקול יש **סליידר אחד בלבד**: `inmarsat-sniffer`
  לא חושף את שלב ה-RF כפרמטר במצב ידני (אומת ב-`sdrplay.c`). ⚠ `gain:null`
  הוא **AGC מפורש** ולא "לא נשלח" — הזיהוי לפי נוכחות המפתח, אחרת אי אפשר
  היה לחזור מרווח ידני ל-AGC.
- **📊 ספקטרום ערוץ ולוג המפענח ב-SATCOM — הפרדה בין "אין RF" ל"כיוון שגוי".**
  עד עכשיו `lock`/Eb·No היו החיווי היחיד, והם מציגים **בדיוק אותו דבר** ("אין
  נעילה") עבור שלוש תקלות שונות לגמרי: אנטנה מנותקת, LNA בלי מתח, וכיוון שגוי.
  כלומר מי שלא קיבל נעילה מעולם לא היה לו שום כלי להבין באיזו מהן הוא נמצא.
  נוסף דגל `--spectrum` למפענח (**עלות CPU רציפה: אפס** — `web_get_spectrum_by_
  channel` קורא את מצב הדמודולטור הקיים, בלי ring buffer ובלי FFT מתמשך; אומת
  מ-`web.c`/`main.c`), ומעליו `GET /api/satcom/spectrum` + תצוגת ספקטרום
  ב-`#satcomAimPanel` עם **רצפת רעש (חציון) ושיא ב-dB**. §12 נשמר: מציגים את
  ה-dB **הגולמי מהמפענח** ולא ממציאים סף "טוב/רע" — הרמז בממשק הוא הוראת-פעולה
  (נתק את ה-LNA וראה אם הרצפה זזה), וההשוואה שהמשתמש עושה מול עצמו היא המדידה.
  ניתן לכיבוי דרך `POST /api/mode {mode:"satcom", spectrum:false}` (נשמר ונזכר
  ב-state כמו `bias_tee`/`skip_c`), כי הדגל מוסיף גם `GET /api/tune` (משנה-מצב)
  לפורט האבחון הלא-מאומת — ר' §9.
- **📜 `GET /api/satcom/log` + כפתור "לוג המפענח" בתצוגת SATCOM.** שורות הפתיחה
  של `inmarsat-sniffer` הן האבחון החד-משמעי ביותר שיש: `sdrplay: bias tee
  enabled` מול `bias tee not supported on this model` (`sdrplay.c`) עונה
  **בוודאות** אם ה-LNA בכלל מקבל מתח, ו-`Satellite:`/`Auto center freq`/
  `Active channels` מאשרות שהתוכנית שנטענה היא זו שציפינו לה. בשטח, מהטלפון,
  אין SSH — בלי זה אי אפשר היה לראות את זה כלל. **על דרישה בלבד** (לחיצת
  כפתור), לא בפולינג: כל קריאה היא fork ל-`journalctl`, וה-health כבר רץ בקצב 1s.
- **⚠ תיעוד: הזנת ה-LNA היא החשוד מספר 1 ל"מעולם לא קיבלתי נעילה".** ה-bias-T
  של ה-RSP1B נותן **100mA**, ו-**Nooelec SAWbird+ iO צורך ‎~180mA נומינלית**
  לפי מפרט היצרן — כלומר **הוא לא נכנס בתקציב הזה**. LNA תת-מוזן לא נכשל בצורה
  ברורה (רווח נמוך / תנודות), והתסמין בממשק זהה ל"אנטנה לא מכוונת". נוסף
  ל-README (טבלת החומרה, מדריך הכיוון, ושורת פתרון-בעיות ייעודית) עם ההפניה
  להזנה חיצונית שכבר קיימת בממשק.
- **⚡ מצב חסכון CPU ל-SATCOM (`skip_c`) — דלוק כברירת מחדל.** ‏Alphasat מגדיר
  12 ערוצים, מהם **6 הם C-channels (OQPSK 8400)**. לפי התיעוד של
  `inmarsat-sniffer` עצמו (`options.c`) הם *"rarely carry ACARS"* וחוסכים
  *"~50% CPU on low-power hosts"* — ו-AIR-AM צורך **ACARS בלבד**, כך שהוויתור
  עליהם כמעט חינם. זהו קו ההגנה העיקרי על תקציב החשמל בהפעלה מסוללה: מדידות
  שדה הראו ש-bias-T + עליית ה-CPU של המפענח יחד מפילים ספק של 5V/3A לגמרי.
  ‏`POST /api/mode {mode:"satcom", skip_c:false}` מחזיר את כל 12 הערוצים;
  הבחירה נשמרת ונזכרת ב-state (כמו `bias_tee`/`satcom_freqs`), עם מתג בתצוגת
  SATCOM. ⚠ ה-auto-enable של הדגל במקור עטוף ב-`#ifdef HAVE_RTLSDR` ⇒ ב-SDRplay
  הוא לא נדלק לבד וחייבים להעביר אותו במפורש (זה מה שנוסף כאן).
- **מכשיר כיוון אנטנה ל-SATCOM.** פאנל ייעודי וגלוי-תמיד בתצוגת SATCOM (לא
  מוסתר כשה-controls נעלמים ב-standby): Eb/No מיטבי (המקסימום בין הערוצים —
  זה מה שממקסמים תוך כדי סיבוב האנטנה) עם גרף היסטוריה (60 דגימות, סקאלה
  קבועה 0–15dB כדי שרעש זעיר לא ייראה כאות חזק), ופירוט פר-ערוץ (baud/lock/
  Eb·No/הודעות/גיל). צביעה **לפי `lock` בלבד** — לא לפי סף Eb/No מומצא (אין
  סף מוסמך במעלה הזרם להעתיק; גם `inmarsat-sniffer` עצמו צובע לפי פעילות
  הודעות, לא סף — ר' §12). `ebno=0 && !lock && mse≈1.0` (חתימת "אין דמודולטור
  פעיל" מהמקור) מוצג כ-"—" ולא כ-"0.0 dB" מוטעה. רענון 1s (במקום 5s) בזמן
  שהתצוגה פעילה + קריאה מיידית בכניסה אליה — לא ממתינים למחזור polling ראשון.
- **הגנת מתח: אזהרה + הצעה (לא חסימה).** הדיאלוג החוסם לפני כניסה ל-SATCOM
  (`confirmSatcomAntenna`) מזהיר עכשיו במפורש כשזוהתה צניחת מתח/throttling
  (`/api/power`) ומקשר אותה לסיכון האמיתי — bias-T + עליית ה-CPU של
  `inmarsat-sniffer` יחד עלולים לכבות את ה-Pi (תרחיש שנצפה בפועל מ-power bank
  שנותן 5V/3A בלבד; ה-Pi 5 תמיד מבקש 5V, ופרופילי 9V/12V של הבנק לא רלוונטיים).
  ההחלטה נשארת של המשתמש — אין חסימה. שורת הצעה לא-חוסמת בתצוגת SATCOM, ליד
  ה-checkbox "הזנת LNA חיצונית": מופיעה כשזוהה undervoltage והוא לא מסומן,
  עם כפתור בנקישה אחת שמסמן אותו.
- **עמידות לניתוק חשמל.** `_atomic_write` עושה עכשיו `fsync` על קובץ ה-tmp
  *וגם* על תיקיית היעד (לא רק `rename` אטומי) — בלי זה נתונים יכולים לשבת
  ב-page cache בלבד וללכת לאיבוד בכיבוי פתאומי (התרחיש שקרה בפועל). ניקוי
  קבצי `.tmp` יתומים מכתיבה שנקטעה, בעלייה בלבד, בכל הספריות שנכתבות
  אטומית (`state.json`, קונפיג הקול, `acars/vdl2/satcom.env`, תמלולי הקלטות).
  `load_state` מבחין בין קובץ חסר (התקנה טרייה — לא אירוע) לקובץ פגום: לוג
  אזהרה + עותק `state.json.corrupt` לאבחון, פעם אחת לאירוע (לא לכל קריאה —
  הפונקציה נקראת גם מראוטים ב-polling תכוף).
- **מד שדה מאוחד + בדיקת אנטנה (`GET /api/signal`, `POST /api/antenna/check`).**
  ב-ACARS/VDL2 אין מדד אות רציף כמו בקול — המפענחים לא חושפים רצפת רעש כששקט,
  רק בתוך הודעה מפוענחת, כך שאי אפשר להבחין בין "אנטנה מנותקת" ל"תקין, שקט
  כרגע" (בדיוק הפער שנפתר ל-SATCOM בלבד ב-2.6.0). `/api/signal` מחזיר את המדד
  הטוב ביותר הזמין למצב שרץ *בפועל*: רציף בקול, "הודעה אחרונה בלבד" ב-VDL2
  (עם SNR אמיתי) ו-ACARS (`level` בלבד, **לעולם בלי SNR** — `acarsdec` לא מודד
  רצפת רעש, ר' §12). ‏"🔍 בדוק אנטנה" עוקף את המגבלה: מעבר זמני (~3 שניות)
  לקול (AGC, סקוולץ' פתוח) בתדר המבוקש, מדידה אמיתית, וחזרה אוטומטית למצב
  שהיה פעיל קודם (כולל best-effort גם כשהמעבר עצמו נכשל) — לא נוגע ב-state.json
  (פעולת אבחון, לא בקשת מעבר-מצב). "📏 כייל בסיס" שומר את התוצאה
  (`state["signal_baseline"]`) כבסיס ההשוואה היחיד לפסקי-דין עתידיים —
  ‏`DISCONNECT_DROP_DB`=10dB אינו סף "איכות" מומצא אלא תצפית פיזיקלית (ניתוק
  אנטנה מנתק מרעש-סביבה ומשאיר רק רעש-פנים, נמוך בהרבה); בלי כיול מפורש —
  `verdict:"no_baseline"`, לא ניחוש (§12). פאנל חדש ב-📻/📡/🛰️ (לא נוגעים
  ב-`#satcomAimPanel` הקיים).
- **דוח סשן: "מה קרה בזמן שלא הסתכלת" (`GET /api/session`, `POST
  /api/session/ack`).** ‏`_boot_restore`/סריקה/שרידות reboot בנויים כדי
  שהתחנה תרוץ לבד לאורך זמן — בלעדי הדוח הזה המוצר היחיד שלה הוא פיד שדורש
  נוכחות רציפה. כרטיס במסך הבית (מעל 10 דק' היעדרות + תוכן ממשי בלבד) מסכם:
  כמות הודעות/מטוסים (וכמה **חדשים** — לא נראו לפני החלון, נגזר מהשוואה
  ל-jsonl שלפני הסמן), עד 8 **הודעות בולטות** (ר' `_interest_score` למטה),
  והחלפת מסלול פעיל/שינוי משמעותי בשיבוש GPS (`adsb.session_series` — סדרה
  בזיכרון בלבד, 6 שעות אחורה, נפרדת מ-`gps_hist` הקצר-טווח של 15 דק'). קורא
  מהדיסק (jsonl), לא מהזיכרון — עקבי עם `?day=` וזמין גם מיד אחרי restart.
  ‏idempotent: `GET` לא מקדם את הסמן; "✓ הבנתי" (`/api/session/ack`) עושה
  זאת במפורש כדי שרענון/פתיחה חוזרת לא "יצרכו" את הדוח בטעות.
- **ציון מעניינוּת (`_interest_score`) + שדה `notable`.** כל הודעה מנורמלת
  (ACARS/VDL2/SATCOM) מקבלת `notable: bool` — לא ציון מספרי מומצא, קריטריונים
  בינאריים משדות שכבר קיימים בכרטיס: קטגוריה לא-גנרית (לא comm/text), טקסט
  מפוענח, מיקום ADS-C, או label עשיר-תוכן (A3/C1/15/16/1L). מזין גם את דוח
  הסשן וגם את ההתראות.
- **🔔 התראות מקומיות על הודעות בולטות.** "הפעל התראות" בכרטיס דוח הסשן
  מבקש הרשאת `Notification` ומאז כל הודעה עם `notable:true` ב-ACARS/VDL2/
  SATCOM (`onMessage` בפקטורי `createDataView` — VDL2/SATCOM לא קיבלו hook
  כזה קודם) מציגה התראה, בקצב מוגבל (לא יותר מאחת כל 4 שניות). מקומי
  (Notification API, לא Web Push/VAPID) — עובד כל עוד הטאב/PWA פתוחים ברקע;
  הרחבה עתידית ל-Push אמיתי לא דורשת שינוי במנוע הניקוד.
- **שכבת רשת: timeout + חיווי ניתוק גלובלי.** כל בקשת רשת בקליינט (16 אתרי
  `fetch`) עוברת עכשיו `fetchTimeout`/`fetchJSON` עם `AbortController` — בלי
  זה בקשה תקועה (Wi-Fi בקצה טווח, Pi שנתקע) הייתה מקפיאה פיד/כפתור בלי הגבלת
  זמן ובלי חיווי, בדיוק המצב שנראה בשטח "תקין" כשהוא לא. צ'יפ חדש ברצועת
  הסטטוס ("אין קשר לשרת") מופיע רק אחרי שקט ארוך (12 שניות) מ*כל* הבקשות
  יחד — לא תלוי בתצוגה/מצב, ולא מהבהב מכשל חד-פעמי של endpoint נדיר.
- **מסמך אסטרטגי: `docs/field-station-roadmap.md`.** תיעוד ההחלטה מאחורי
  שני הפיצ'רים לעיל — ארבע עמדות עיצוביות שנבחנו (אנליטיקה מהנתונים / אותות
  RF חדשים / אמינות / UX שטח), ההתכנסות המשותפת שלהן (אין דרך להבחין "אנטנה
  מנותקת" מ"שקט" מחוץ ל-SATCOM), ומה נדחה במפורש ולמה (SQLite, חיזוי, ADS-B
  מקומי, לוויני מזג אוויר, החלפת מצב בהחלקה).

### שונה
- `GET /api/power` מוגש מ-cache בן ~2 שניות (היה מריץ 3 תהליכי `vcgencmd`
  בכל בקשה; עם כמה טאבים פתוחים כל אחד עם `pollPower` כל 5 שניות, זה הצטבר
  למספר לא מבוטל של תהליכים בשנייה — מורגש דווקא בהפעלה מסוללה).

### תוקן
- **`pollPower`: כשל בקריאת `/api/power` השאיר את הצ'יפ תקוע על "מתח תקין"
  לנצח** (catch ריק) — עכשיו מסמן מצב לא-ידוע אחרי כשל, מסוכן במיוחד בשטח.
  גם: בענף ה"בעייתי" הטמפרטורה לא הוצגה, בניגוד לענפים האחרים.
- **פאנל הכיוון פילל את `/api/satcom/health` כל שנייה גם כשהמקלט כבוי** —
  וכל בקשה כזו מריצה `systemctl is-active` (subprocess) בשרת. עיון בהיסטוריית
  SATCOM ב-standby שרף fork בשנייה על ה-Pi בלי שום תמורה — בדיוק תקציב החשמל
  שאנחנו מנסים לשמור. עכשיו מפללים רק כשהמקלט פעיל בפועל; הדלקה ממקום אחר
  (טלפון שני/⏻) נתפסת ב-`pollGlobalState` שמתניע את הפאנל מיד.

## [2.7.0] - 2026-07-31
### נוסף
- **הזנת LNA חיצונית למצב SATCOM (`bias_tee`).** ה-bias-T של ה-RSP1B מוגבל
  ל-100mA, ותוספת הצריכה שלו יחד עם עליית ה-CPU של `inmarsat-sniffer`
  בו-זמנית עלולה לדחוף ספק כוח שולי (נצפה בפועל: power bank נייד עם USB-C
  PD שנותן 5V/3A בלבד — פרופילי 9V/12V שלו לא רלוונטיים כי ה-Pi 5 תמיד
  מבקש 5V) מעבר לתקרה, ולגרום לכיבוי מלא של ה-Pi (לא reboot רך). `POST
  /api/mode {mode:"satcom", bias_tee:false}` מכבה את bias-T של ה-RSP1B —
  המשתמש מזין את ה-LNA ממקור נפרד (USB power bank + DC injector). נשמר
  ונזכר ב-state (כמו `satcom_freqs`). checkbox חדש "🔌 הזנת LNA חיצונית"
  בתצוגת SATCOM, מאותחל בטעינת הדף (לא רק בביקור בתצוגה — `enterSatcom()`
  נקרא גם מכרטיס הבית/כפתור ⏻). דיאלוג האישור (`confirmSatcomAntenna`)
  מציג טקסט שונה בהתאם — לעולם לא טוען ש-bias-T ידלק כשהוא לא ידלק.
  ⚠ אחריות המשתמש: לא להזין משני מקורות בו-זמנית.

## [2.6.0] - 2026-07-31
### נוסף
- **אבחון SATCOM: `GET /api/satcom/health`** — proxy מקומי ל-dashboard האבחוני
  המובנה של `inmarsat-sniffer` (‎`--web`, ברירת מחדל פורט 8888): נעילת
  דמודולטור/ebno/mse לכל ערוץ, **גם באפס הודעות מפוענחות** — ההבדל בין "אין
  אנטנה"/"לא מכוון" ל"תקין, שקט כרגע" ש-`_is_active` בלבד לא יכול להבחין בו.
  בתצוגת SATCOM מוצג עכשיו "מפענח: נעולים X/Y ערוצים" כשהמקלט פעיל. ⚠ הפורט
  נקשר ל-INADDR_ANY (לא ניתן להגבלה ל-loopback דרך הכלי) — נגיש ברשת המקומית,
  אותה קטגוריית סיכון כמו Icecast (ר' §9 ב-CLAUDE.md).

### תוקן
המשך הביקורת שהניבה את תיקוני 2.5.0 — פערים נוספים שנמצאו:
- **`error` ב-SATCOM כבר לא תמיד `0`.** אומת מהמקור: `isu.acars` החיצוני
  (feed.c) לעולם לא כולל `err`/`crc_ok`, וה"שער" בפועל (`main.c`) בודק
  reassembly+`err` — לא `crc_ok` — כך שהודעה עם CRC כושל יכולה לעבור. `crc_ok`
  אמיתי קיים רק במעטפת הפנימית הכפולה (כשיש יישום ARINC-622/ADS-C מקונן) —
  עכשיו נקרא משם כשזמין, בדיוק כמו VDL2; ליתר ההודעות (טקסט רגיל) `error`
  נשאר `0` (לא מומצא — פשוט לא ידוע).
- **תוקנה הערה שגויה בקוד:** נטען ש-AES/GES "אינם ICAO 24-bit" — אבל
  `inmarsat-sniffer` עצמו ממפה אותם כ-hex זהה ל-ICAO (`aircraft_db_lookup_by_aes`,
  אומת מהמקור). ההחלטה בקוד (לא למפות ל-roster) נשארה — רק הנימוק תוקן.
- **בדיקות רגרסיה חסרות שהושלמו:** `voice`/`vdl2` עוצרים `satcom` (היה רק
  ל-`off`/`acars`), לוח סריקה עם רגל `satcom` נדחה (הקוד כבר חסם — לא היה
  טסט), וארכיון `?day=` ל-`/api/satcom`.
- **תוקן: מתג-המצב העליון (`#modeSeg`) חפף את כפתור ⏻ במובייל.** מאז שנוסף
  כפתור חמישי (SATCOM), חמש תוויות טקסט מלאות + "▶ הפעלה" (המחרוזת הארוכה
  של כפתור ⏻ ב-standby) לא נכנסו יחד ברוחב מסך טלפון — `.seg-modes` גלש
  חזותית ("SATCOM" חפף את כפתור ⏻), במיוחד כשהמקלט ב-standby. אומת ותוקן
  בפועל בדפדפן (Chromium, viewport 390px): כל חמשת כפתורי המתג וגם כפתור
  ⏻/הפעלה מצטמצמים לאייקון-בלבד במובייל (‎`.mode-lbl`/`.pwr-lbl` מוסתרים,
  `title` נשאר לנגישות) — בדיוק כמו שכבר נעשה לכפתור "מרכז". דסקטופ/טאבלט
  לא השתנו (עדיין תוויות מלאות).

## [2.5.0] - 2026-07-31
### נוסף
- **מצב `satcom` — קליטת ACARS דרך לוויין Inmarsat (L-band)**, צרכן SDR רביעי
  שווה-מעמד לצד voice/acars/vdl2. מפענח: `inmarsat-sniffer` (alphafox02) —
  בינארי CLI עצמאי (בלי Qt/GUI, בניגוד ל-JAERO המקורי), נעוץ ל-commit ידוע-טוב
  (אין releases רשמיים לפרויקט). הפלט מסונתז ל-dict בסגנון acarsdec ומוזרם דרך
  `_normalize_acars` הקיים — בדיוק כמו מסלול A של VDL2 — כך שכל הפרסרים
  (ATIS/OOOI/PDC/label-15/16/H1/ARINC-622 כולל ADS-C), הרוסטר המאוחד, המפה
  והארכיון חלים עליו בחינם. `airam-satcom.service` חדש (Conflicts דו-כיווני מול
  שלושת הצרכנים האחרים, לא enabled — כמו כל צרכן SDR). לוויין ברירת מחדל:
  `AF1` (Alphasat, +25.0E) ל-EMEA/ישראל.
- **חומרה נדרשת (נפרדת מאנטנת ה-airband): אנטנת L-band RHCP + LNA** (למשל
  Nooelec SAWbird+ IO), מוזן מ-bias-T של ה-RSP1B (‎-B, מודלק *רק* במצב satcom).
  החלפת האנטנה בין VHF ל-L-band **ידנית** — ה-UI מציג באנר-הוראה בכניסה/יציאה
  ממצב satcom. פירוט מלא (BOM, כיוון אנטנה, סכמת JSON מאומתת מהמקור) ב-
  `docs/satcom-feasibility.md`.
- `GET /api/satcom` + `/api/satcom/export` (אותה סכמה כמו `/api/acars`, בלי
  `level`/`snr`/`freq` — inmarsat-sniffer לא חושף אותם ב---feed/--udp, ולכן
  לעולם לא מומצאים — ר' §12 ב-CLAUDE.md).
- **UI:** תצוגת SATCOM רביעית (מופע נוסף של פקטורי `createDataView` — פיד/מפה/
  רוסטר/ארכיון/ייצוא בחינם), כרטיס הפעלה במסך הבית, בורר-לוויין (במקום בורר-
  בנקים), ובאנר-הוראה קבוע על החלפת אנטנת ה-L-band הידנית + תזכורת בעצירה.
  ‏satcom **אינו** נכלל בעורך הסריקה (MVP מכוון). אמוג'י 📶 (‏🛰️ תפוס ע"י VDL2).

### תוקן
בדיקה יזומה של מצב `satcom` (הפיצ'ר הקודם) חשפה מספר בעיות אמינות/בטיחות
ובאגי נרמול — כולן תוקנו עם בדיקות מתאימות:
- **⚠ בטיחות: bias-T לא נדלק יותר אוטומטית ללא פיקוח אנושי.** `_boot_restore`
  כבר לא נכנס אוטומטית ל-satcom אחרי `reboot` (נופל בכוונה ל-`off` עם
  `prev_mode=satcom`) — אחרת ה-bias-T (‎+4.7V על מחבר האנטנה) היה נדלק בלי
  שאף אחד יוודא איזו אנטנה מחוברת. כל נקודת כניסה ידנית ל-satcom מה-UI (כרטיס
  הבית, כפתור ⏻, כפתור ההפעלה בתצוגת SATCOM) עוברת עכשיו דרך דיאלוג אישור
  חוסם *לפני* השליחה לשרת, לא רק הבאנר הקבוע בתצוגה. בנוסף, `airam-satcom.service`
  מקבל תקרת-הפעלות סופית (`StartLimitBurst`, בניגוד ל"מתאושש לנצח" של שאר
  צרכני ה-SDR) — קריסה חוזרת של `inmarsat-sniffer` כבר לא ממשיכה להדליק את
  ה-bias-T שוב ושוב; `_enter_satcom` מנקה את התקרה (`systemctl reset-failed`,
  sudoers ממוקד) לפני כל כניסה כדי שהפעלה ידנית תמיד תעבוד.
- **תוקן: כפתור ⏻ לא החזיר ממצב standby ל-SATCOM.** עצירה ואז לחיצה חוזרת על
  כפתור ההפעלה ניווטה למסך הבית בשקט במקום להדליק SATCOM מחדש — שאר המצבים
  (voice/acars/vdl2/scan) כבר טופלו נכון.
- **תוקן: `decoded` הציג טקסט גולמי כפול/מטעה עבור הודעות ADS-C/CPDLC ב-SATCOM.**
  ‏`inmarsat-sniffer` עוטף מחדש את *כל* עץ ה-ACARS בתוך `isu.acars.arinc622`
  (אומת מהמקור: `main.c`), לא רק את היישום המקונן כמו VDL2 — `_normalize_satcom`
  לא פירק את השכבה הכפולה, כך שה-UI הציג את `msg_text` הגולמי כאילו הוא תוכן
  מפוענח. הפיקסצ'ר בבדיקות תוקן לשקף את המבנה האמיתי (במקום מבנה "נקי" לפי
  הנחת המימוש) כדי שרגרסיה עתידית תיתפס.
- **תוקן: מיקום עלול היה להיות מיוחס בטעות מתוך CPDLC** (waypoint מוטבע ב-
  clearance, לא מיקום המטוס עצמו) ב-`_normalize_acars` המשותף — הגנה מקבילה
  כבר קיימת ב-VDL2 מסלול B, עכשיו גם ב-ACARS/VDL2-A/SATCOM. מיקום מיוחס
  *רק* מ-ADS-C.
- **תוקן: `SATCOM_UDP` נכתב ל-`satcom.env` אך מעולם לא נקרא** — `ExecStart`
  קידד את הפורט בקשיחות; שינוי עתידי בפורט היה משאיר את הקונפיג הכתוב לא-
  בשימוש בשקט. עכשיו `${SATCOM_UDP}` בפועל, כמו `ACARS_UDP`.
- **תוקן: `install.sh` שמר סימן-בנייה "הצליח" גם כשנבנה בלי תמיכת SDRplay** —
  הרצה חוזרת אחרי התקנת SDRplay API לא בנתה מחדש בפועל. עכשיו סימן-הבנייה
  נכתב רק כשתמיכת SDRplay אושרה בלוג ה-cmake.
- **תוקן: כשל בעצירת standby דיווח תמיד journal של `rtl_airband`**, גם כשהשירות
  התקוע היה acars/vdl2/satcom — בדיוק כשהאבחון הכי חשוב (bias-T עדיין דלוק).

## [2.4.0] - 2026-07-09
### תוקן
סקירת קוד יזומה (ללא באג ספציפי שדווח) חשפה מספר בעיות אמינות במצב הסריקה
ובחיווי ה-UI. הבאגים המרכזיים תוקנו יחד עם בדיקות מתאימות:
- **חלון שעות שנסגר באמצע סבב סריקה לא כיבה את הצרכן הרץ** — `_scan_loop`
  עדכן רק את `_scan_status` (החיווי), אבל הצרכן (rtl_airband/acarsdec/dumpvdl2)
  של הרגל הקודמת המשיך לרוץ בפועל. עכשיו כשסבב שלם עובר בלי אף רגל בחלון,
  הצרכן הרץ נכבה בפועל (`_enter_standby`) — לא רק נעלם מה-UI.
- **"scan זומבי"** — `/api/mode` עצר סבב סריקה פעיל *לפני* שידע אם הבקשה
  החדשה בכלל תצליח; לוח לא-תקין (400) או `TUNE_LOCK` תפוס (409) היו משאירים
  את המערכת תקועה על הרגל האחרונה בלי סבב שממשיך אותה. הוולידציה הסטטית
  (פרסור פרמטרים/לוח/תדרים) קודמת עכשיו לעצירת הסבב, ותפיסת `TUNE_LOCK`
  מקבלת timeout קטן (0.5ש') כדי לספוג תחרות-עצמית מול הסבב בלי 409 מיותר.
- **לוח סריקה עם רגל יחידה (או רגלים חוזרות) גרם ל-restart מיותר של השירות
  בכל dwell** — `_scan_loop` לא בדק אם הרגל הבאה זהה בדיוק (מצב+תדרים) לרגל
  שכבר רצה; עכשיו רגל זהה רק מרעננת את הטיימר, בלי `systemctl restart`.
- **`_boot_restore` יכול לדרוס בחירת מצב טרייה של המשתמש** — בין קריאת ה-state
  בתחילת הפונקציה להמתנה של עד 90 שניות ל-SDR, אם המשתמש הספיק לבחור מצב אחר
  מה-UI (ואותה בחירה כבר הסתיימה), ה-state הישן היה דורס אותה. השחזור קורא
  מחדש את ה-state אחרי תפיסת הנעילה ומוותר אם הוא השתנה בינתיים.
- **`_day_bounds` (ארכיון חיפוש) שגוי בימי מעבר שעון קיץ/חורף** — `start+86400`
  הניח יום של 24 שעות בדיוק; ביום של 25/23 שעות הודעות מהשעה הגבולית נעלמות
  מהארכיון או מוכפלות בין יומיים. עכשיו `end` מחושב עם `mktime` על היום הבא —
  חצות אמיתי, לא הפרש קבוע.
- **`_leg_active_now` עם `active_from == active_to`** נחשב "אף פעם" במקום
  "תמיד" (24 שעות) — תוקן.
- **`_validate_scan_plan`** זרק 500 (TypeError) על `active_from`/`active_to`
  שאינם מחרוזת (JSON תקין, למשל מספר) — מחזיר עכשיו 400 מסודר.
- **ACARS/VDL2 listeners יכלו למות לצמיתות** על חריגה בלתי-צפויה בתוך
  `_normalize_acars`/`_normalize_vdl2` (למשל שדה מטיפוס לא-צפוי) — הפיד היה
  נעצר עד restart של `airam-web`, בלי חיווי ב-`/api/health`. עכשיו כל דאטהגרם
  שנכשל בנרמול נרשם ללוג ומדולג, ה-thread ממשיך לרוץ.
- **`_atomic_write`** השתמש בקובץ `.tmp` קבוע לכל path — שתי בקשות מקבילות
  (למשל שני `PUT /api/presets`) יכלו להתנגש על אותו קובץ ביניים. עכשיו ה-tmp
  ייחודי לפר-thread (pid+ident).
- **UI: `reloadStream`** בדק רק `acars.active` — חזרה לטאב/כפתור "⟳ סנכרן"
  ב-VDL2 או ב-standby הריצו 10 ניסיונות חיבור מיותרים והציגו "הסטרים לא זמין"
  על מצב תקין לגמרי. כפתורי הסטרים (סנכרון/נגן חיצוני) מוסתרים כעת גם הם
  כשה-SDR לא בקול, כמו נגן ההשמעה.
- **UI: כשל רשת ב-`applyMode`** השאיר את התצוגה במצב אופטימי שקרי (למשל
  "מקלט בכיבוי" בזמן שהרדיו ממשיך לשדר) — הדגלים משוחזרים כעת למצב שהיה
  לפני הניסיון.
- **UI: `setStatus(..., "on")`** במעבר לסריקה השתמש במחלקת CSS שלא קיימת
  (הנקודה נשארה אפורה-כבויה) — תוקן ל-`"live"`. כניסה למצב סריקה גם מביאה
  עכשיו `/api/scan` מיד במקום לנחש שרגל 0 רצה (חלון שעות עשוי לשלוח למקום אחר,
  או "לממתין").
- **UI: לוח ATIS/hint טקסט "השמע ממשיך לרוץ"** בתצוגות ACARS/VDL2 היה תמיד
  מניח שה-SDR בקול — שקרי ב-standby או במצב הדאטה האחר; מחושב עכשיו מהמצב
  החי בפועל.
### נוסף
- **ביטול-מחיקה לפריסטים** — טוסט "נמחק · בטל" ל-5 שניות במקום מחיקה מיידית
  וסופית במצב עריכה (אצבע שפספסה "✔ סיום" איבדה פריסט לצמיתות).
- **שורות רוסטר המטוסים במסך הבית לחיצות** — מעבירות לתצוגת המקור (ACARS/VDL2)
  וממקדות על אותו מטוס, במקום לדרוש חיפוש ידני מחדש.
- **כפתורי העתקה (📋)** לטקסט ACARS/VDL2 גולמי ול-METAR.
- **גובה ADS-B ברוסטר** מוצג גם כטקסט בשורה, לא רק כ-`title` (לא נגיש במגע).
- ולידציה בצד הלקוח לעורך לוח הסריקה: הודעת שגיאה על לוח ריק בלחיצת "התחל
  סריקה", ועל רגל עם שדה חלון-שעות בודד (רק "מ־" או רק "עד").
- נגישות מקלדת לצ'יפים לחיצים (סינון קטגוריה, זנב/טיסה בכרטיס הודעה) —
  `tabindex`+`role=button`+Enter/Space.
- שיפורי חיכוך קטנים: `pollActivity` מיידי בכניסה לתצוגת הקול; מתג התצוגות
  (🏠/📻/📡/🛰️) לא ננעל בזמן מעבר SDR (מעבר תצוגה לא נוגע ב-SDR); תאריך מרבי
  בארכיון החיפוש מחושב מחדש בכל כניסה לתצוגה (לא רק בטעינת הדף — PWA שנשארת
  פתוחה מעבר לחצות); לוח ATIS דביק מסומן "ישן" (עמעום) אחרי שעה בלי עדכון.
### שונה
- ניגודיות `--text-faint` הובהרה בשני הערכות הנושא (עומדת ב-AA לטקסט קטן).
- מטרות מגע קטנות (כפתורי ייצוא/ארכיון/מחיקת-רגל) הוגדלו ל-≥36px.
- סיכום לוח הסריקה במסך הבית חוזר ל-`direction:rtl` (היה `ltr` גורף בטעות
  שהפך את סדר הקריאה של הטקסט העברי עם החצים).
- שורת עריכת רגל בעורך הסריקה עוברת ל-`flex-wrap` — בורר המצב כבר לא נמעך
  ללא-שמיש במסכי טלפון צרים (~360px).

## [2.3.0] - 2026-07-06
### נוסף
- **ארכיון חיפוש רב-יומי (ACARS+VDL2)** — `?day=YYYY-MM-DD` חדש ב-`/api/acars`
  וב-`/api/vdl2`: קורא מהדיסק (jsonl) ולא מהזיכרון, מחזיר את כל הודעות אותו
  יום מקומי כ-snapshot סטטי (בלי cursor/adsb). ב-UI: כרטיס ארכיון בשני
  התצוגות (בורר תאריך + "חזרה לשידור חי") — ממומש **פעם אחת בפקטורי
  `createDataView`** (`enterArchiveDay`/`exitArchive`) כך ששתי התצוגות מקבלות
  אותו אפשר לדפדף בהיסטוריה המלאה (לא רק "היום") עם כל מנגנוני החיפוש/סינון/
  roster/מפה הקיימים בחינם — עוצר את הפולינג החי בזמן עיון בארכיון ומשחזר אותו
  במדויק ביציאה. אגב הפיתוח תוקן גם רמז VDL2-ספציפי ("136.975") שדלף לברירת
  המחדל של ACARS בפקטורי המשותף — עבר ל-`opts.emptyHint` (VDL2 בלבד).
- **חלונות שעות פר-רגל במצב סריקה** — כל רגל יכולה לקבל `active_from`/`active_to`
  אופציונליים ("HH:MM", תומך בחלון שחוצה חצות). רגל מחוץ לחלון שלה מדולגת מיד
  (לא כשל); אם אף רגל לא בחלון כרגע, ה-SDR נשאר ב-standby ו-thread ממתין
  (`SCAN_WINDOW_RECHECK_SEC`=30 שניות בין בדיקות אחרי סבב שלם ריק) — **גם זו
  לא תקלה**: `api_state`/`api_health` מדווחים `mode_ok`/`ok=True` כל עוד אין אף
  רגל שאמורה לרוץ כרגע. עורך הלוח במסך הבית מקבל שני שדות `time` אופציונליים
  פר-רגל; מסך הבית מציג "ממתין לחלון הזמן הבא" כשמתאים.

## [2.2.0] - 2026-07-06
### נוסף
- **רוסטר מטוסים מאוחד (ACARS+VDL2+ADS-B)** — `GET /api/aircraft` חדש: מהתך את
  היסטוריות ACARS ו-VDL2 שבזיכרון לפי זהות משותפת (רישום מנורמל קודם, אחרת icao,
  אחרת מספר טיסה) ומעשיר ב-ADS-B חי (`adsb.aircraft_snapshot`) — כל מטוס מקבל
  תגיות מקור (`sources`: acars/vdl2), ספירת הודעות, קטגוריה/מיקום/סוג אחרונים.
  **חי בכל מצב** (גם standby/סריקה) — לא תלוי SDR הפעיל כרגע, כי שני ה-listeners
  ו-thread ה-ADS-B רצים תמיד ברקע. פאנל חדש במסך הבית (`renderRoster`/`pollRoster`,
  כל 20ש', ללא תלות ב-appMode — כמו airspace).
### שונה
- **סימטריית קוד מלאה: ACARS הפך למופע שני של `createDataView`** — פיצ'ר 1
  ממפת הדרכים. הקוד הייעודי של ACARS (~780 שורות: buffers/מפה/roster/פיד/polling
  כפולים) הוסר; `var acars = createDataView({prefix:"acars", mode:"acars",
  label:"ACARS", onMessage, onReset})` מחליף אותו — אותו קוד בדיוק כמו VDL2,
  אפס הבדל התנהגותי. הפקטורי עצמו הוכלל: נתיב ה-API (`/api/vdl2` → `/api/`+prefix),
  טקסט הסטטוס ("VDL2"/"ACARS") ותגית ה-mode ב-`selectBank` פורמלו דרך `opts`
  במקום קבועים; שני hooks אופציונליים חדשים (`onMessage`/`onReset`, no-op
  כברירת מחדל) משמרים את התנהגות לוח ה-ATIS הייחודית ל-ACARS בלי קוד ACARS-only
  מחוץ לפקטורי. שינויי ID ב-`#acarsView` ליישור עם `#vdl2View` (`sdrInactive`→
  `acarsInactive` וכו', `towerBtn`→`acarsTowerBtn`) — סימטריה מלאה בתבנית ה-DOM.
  refactor טהור: אומת ב-Chromium headless (הזרקת UDP סינתטית ל-ACARS/VDL2,
  בדיקת badges/roster/detail/מפה/לוח-ATIS/כשל-מעבר-מצב) — אפס רגרסיה.

## [2.1.0] - 2026-07-06
### נוסף
- **מצב סריקה/סבב 🔁 — מחזור אוטומטי בין המצבים לפי לוח זמנים**, כפיצ'ר שוויוני
  רביעי לצד קול/ACARS/VDL2: `app_mode="scan"` חדש עם "רגלים" (`mode`+`dwell_sec`+
  `freqs?` אופציונלי), עורך לוח (הוספה/הסרה/עריכה) וכרטיס סטטוס חי (רגל נוכחית +
  ספירה לאחור למעבר הבא) במסך הבית. thread נפרד מסתובב בין הרגלים תחת `TUNE_LOCK`
  רק בזמן מעבר (לא בזמן ההמתנה) — עצירה/מעבר מצב ידני מתערבים כמעט מיד. כשל ברגל
  בודדת מדלג לבאה; כשל של *כל* הרגלים ברצף (סבב שלם) נופל ל-`off`, כמו כל מצב אחר
  (אין fallback לקול). סריקה שורדת reboot כמו שאר המצבים (`_boot_restore`). נתיב
  חדש `GET /api/scan` (סטטוס חי לצורך ה-UI).
- **מדדי איכות קליטה בכרטיסי ACARS/VDL2: dBFS + SNR** — `level` (dBFS, מהמפענח
  עצמו) מוצג עכשיו כתווית צבעונית בכל כרטיס הודעה (גם ACARS, גם VDL2). `snr`
  מחושב **רק** כשיש רצפת רעש אמינה: `dumpvdl2` מספק גם `sig_level` וגם
  `noise_level` לכל פריים ⇒ VDL2 מקבל SNR אמיתי (`sig_level - noise_level`).
  `acarsdec` **לא** מספק רצפת רעש לכל הודעה (נבדק במקור: `output.c`) ⇒ הודעות
  ACARS מציגות dBFS בלבד, בלי SNR מומצא/מוערך. dBm נדחה במכוון ל-PR עתידי —
  מחייב מעבר לרווח קבוע (לא AGC) + כיול חד-פעמי, תשתית שאינה קיימת היום.
### תוקן/הרחבה
- `ACARS_EXPORT_COLS`/`VDL2_EXPORT_COLS` כוללות עכשיו עמודת `snr`.

## [2.0.0] - 2026-07-06
### שונה
- **ארכיטקטורת הפעלה שוויונית — אין יותר "מצב ראשי"** (שינוי שובר, ⇒ 2.0.0).
  התפיסה: ה-SDR הוא משאב, קול/ACARS/VDL2 הם "אפליקציות" שוות-מעמד, `off` (standby)
  הוא המצב הניטרלי היחיד, ו-**airam-web הוא המתזמר (orchestrator)**:
  - **המצב שורד reboot**: אף צרכן SDR (כולל `rtl_airband`!) אינו `enabled` יותר —
    `airam-web` קורא את `state.json` באתחול ומשחזר את המצב השמור האחרון
    (`_boot_restore`, thread שלא חוסם את השרת), כולל `off`. עד היום כל reboot נחת
    בקול; עכשיו המערכת קמה במצב שבו כיבית אותה.
  - **אין fallback לקול**: כישלון כניסה לכל מצב (כולל קול, אם גם הרולבק לתדר הקודם
    נכשל) נופל ל-`off` עם שגיאה ברורה ב-UI — במקום "לחזור לקול" בשקט. הרולבק
    בתוך כיוונון קול נשאר (retry של קונפיג הקול האחרון שעבד), אבל עכשיו **מאומת**
    (`_rollback` מחזיר הצלחה/כישלון).
  - **`api_state`/`api_health` בלי הטיית קול**: המצב נגזר מהשירות הפעיל, ובאין
    פעיל — מהכוונה השמורה. מצב שמור שלא רץ = תקלה מדווחת (`mode_ok=False` /
    `ok=False`), לא טענת-"voice" שקטה. ברירת המחדל של state חסר/פגום היא `off`.
  - **backend סימטרי**: `_enter_voice` חדש (peer של `_enter_acars`/`_enter_vdl2`),
    `MODE_SERVICE`/`_live_mode`/`_fail_to_off`, ו-`/api/mode` אוחד ל-dispatcher
    אחד (ארבעת הענפים המשוכפלים + שני בלוקי ה-fallback-לקול נמחקו).
  - **עצירה = standby**: כפתורי "⏹ עצור / חזור לקול" ב-ACARS/VDL2 הפכו ל"עצור
    (Standby)" ונוחתים במסך הבית. כפתור ההדלקה (⏻) מחזיר את **המצב האחרון**
    (`prev_mode` נשמר ב-state) — לא קול.
  - **install.sh**: `systemctl enable` בלי rtl_airband + `disable` אידמפוטנטי בשדרוג;
    הוסר ה-restart הגורף של rtl_airband בסוף ההתקנה (שהעיף משתמשי ACARS/VDL2 לקול
    בכל שדרוג) — `restart sdrplay` + `PartOf` מרימים את הצרכן הפעיל, יהיה אשר יהיה.
### נוסף
- **מסך בית 🏠 (מרכז הבקרה)** — תצוגה רביעית ניטרלית, ברירת המחדל והנחיתה של מצב
  `off`: כרטיס סטטוס חי, שלושה **כרטיסי הפעלה שווי-מעמד** (📻/📡/🛰️ עם התדר/הבנק
  השמורים), הצגת שגיאות מעבר-מצב, ופאנלי **המסלולים הפעילים וה-METAR** שעברו אליו
  מתצוגת הקול (מודעות-מצב מערכתית, לא של קול). ה-pollers של מרחב-אווירי/מתח/METAR
  שוחררו משער תצוגת-הקול ורצים בכל תצוגה.
- **poll יישור גלובלי** (כל 10ש', `/api/health`) — ה-UI מיישר את חיווי המצב למציאות:
  מצב ששוחזר באתחול, שינוי חיצוני (`systemctl`) או קריסת שירות, בלי לחטוף את הטאב.
- **שדות API חדשים**: `mode_ok` ב-`/api/state` (המצב השמור באמת רץ?) ו-`prev_mode`
  (המצב האחרון לפני `off`, לכפתור ההדלקה).
- **בדיקות**: `tests/test_boot.py` (11 בדיקות ל-`_boot_restore`), בדיקות נפילה-ל-off,
  חוזה תשובת הכישלון של `/api/mode`, `mode_ok`, ותקלת "מצב שמור שלא רץ" ב-health.
### הוסר
- שני בלוקי ה"חזרה לקול" ב-`/api/mode` (כישלון ACARS/VDL2), ברירת המחדל `voice`
  ב-`api_state`/`api_health`, וה-`[Install]` של `rtl_airband.service`.
### תוקן
- **`airam-vdl2.service` נכשל לעלות תמיד** — ה-`ExecStart` השתמש בדגל `--sample-rate`
  שאינו קיים ב-dumpvdl2; הפרסר הפנימי שלו מדפיס usage ויוצא מיד (נראה בלוג כ"נכשל
  לעלות" / "Deactivated successfully" אחרי הדפסת ה-help). קצב הדגימה של dumpvdl2 נגזר
  מ-`--oversample` (`sample_rate = 105000 * oversample`); הוחלף ב-`--oversample 20`
  שנותן את אותם 2.1MS/s שהיו מיועדים.

## [1.15.0] - 2026-07-06
### נוסף
- **מבנה "חדר בקרה" — App Shell + גריד דשבורד** (רסטרקטורינג ויזואלי של `static/index.html`;
  ה-JS לא שונה מלבד שתי תוספות ResizeObserver, וכל ה-IDs נשמרו — החוזה של `$()` נשמר במלואו):
  - **סרגל-על דביק (App Bar)** מלא-רוחב עם זכוכית מטושטשת: לוגו+שם+גרסה, מתג-המצב
    (📻/📡/🛰️) ממורכז בתוכו, וכפתור הכיבוי בקצה. מחליף את ה-`<header>` וה-`.modebar` הנפרדים.
  - **רצועת סטטוס (Status Strip)** תמיד-גלויה מתחת לסרגל, עם נורות LED חיות ברמת-מערכת:
    סטטוס שירות (`#pill`), מתח ה-Pi (`#pwrChip`) ושיבוש GPS (`#gpsPill`) — כולם *הועברו*
    לכאן ממקומם הקודם (בלי שינוי JS, גישה לפי ID). הפיל הפך לחיווי גלובלי הגלוי בכל המצבים.
  - **גריד דשבורד** בתצוגת הקול: 7 פאנלים (`.panel` עם `.panel-hd`/`.panel-bd`/`.panel-ft`)
    בפריסת `grid-template-areas` רספונסיבית (עמודה במובייל, 2 עמודות ב-≥700px, 3 ב-≥1440px).
    מדדי ה-RF חולצו לפאנל נפרד; שורת הכיוונון עברה ל-footer של פאנל הרדיו; היומן הפך
    לקונסולה גוללת בתוך העמודה שלו בדסקטופ.
  - **תצוגות ACARS/VDL2 כפאנלים**: פאנל בקרת-מקלט, **רצועת סטטיסטיקה ברוחב מלא**, ופאנל
    מפה (דביק) לצד פאנל פיד עם כותרת+כלים. שתי התצוגות מבנה זהה (מירור, פרט ל-ATIS ש-ACARS
    בלבד); `#acarsFeed`/`#vdl2Feed` נשמרו כקונטיינרי-גלילה ייעודיים (חוזה ה-`replaceChildren`).
  - **שפת מכשור**: קריאוֹת מספריות (תדר, RF, gain, מסלולים, סטטיסטיקה, זמנים) במונו-ספייס
    עם `tabular-nums`; נורות LED עם זוהר עדין במצב פעיל; פינות מהודקות (רדיוסים 12/10/8px);
    מספר-התדר והמסלולים מוצקים (בלי גרדיאנט) בסגנון לוח-בקרה.
  - **ResizeObserver** על שתי מפות ה-Leaflet ⇒ `invalidateSize()` גם כשגודל פאנל-המפה
    משתנה בלי resize-חלון (reflow גריד) — האריחים לא נחתכים.
- **מצב בהיר אוטומטי** — ה-UI תומך כעת גם ב-light theme לצד הכהה, עובר ביניהם אוטומטית
  לפי `prefers-color-scheme` של המערכת (בלי כפתור מעבר ידני). `color-scheme: light dark`;
  שני תגי `theme-color` מותנים-מדיה; מפת Leaflet מציגה אריחי OSM רגילים במצב בהיר במקום
  ההיפוך הכהה (שנשאר ברירת המחדל לדפדפנים ללא תמיכה ב-`prefers-color-scheme`).

### שונה
- **עיצוב מודרני-מינימליסטי בהשראת NotebookLM** (שינוי ויזואלי טהור ב-`static/index.html`,
  ללא נגיעה במבנה ה-DOM או ב-JS): פלטה נייטרלית ומרוככת יותר (פחות כחול-שחור), השטחת
  כרטיסים/כפתורים/מצבי-פעיל (הוסרו גרדיאנטים דו-עצירה לטובת מילוי שטוח + קו-מתאר עדין),
  צללים מרוככים ופחות "זוהרים", טיפוגרפיה מאופקת יותר (כותרת/מספר-תדר `700` במקום `800`,
  ריווח-אותיות מוקטן), וסקאלת המרווח `--sp-1..5` (שהוגדרה אך לא נוצלה) חוברה בפועל לכרטיסים/
  פריסה/כפתורים/הגדרות. כל צבעי הבדג'ים/פילים/הודעות ה-ACARS-VDL2 עברו לטוקנים
  (`rgba(var(--accent-rgb),..)` וכו') כדי שיתעדכנו אוטומטית בשני המצבים במקום להישאר
  מוקשחים לגוון הכהה.
- **ריענון עיצוב — פרופורציות ואלגנטיות** (סקירת נראות של שלוש התצוגות, בלי לשבור
  התנהגות/RTL; ה-UI נשאר `static/index.html` יחיד ותצוגת VDL2 ממשיכה להשתמש חוזר
  במחלקות ה-`.acars-*`):
  - **מערכת טוקנים חדשה ב-`:root`**: סקאלת גיאומטריה אחידה (`--r-card` 16px /
    `--r-ctrl` 12px / `--r-sm` 10px), סקאלת מרווח (בסיס 4px: `--sp-1..5`), והצללה
    מרובדת ורכה (`--sh-card`/`--sh-1`/`--sh-blue`) במקום צל שחור אחיד וכבד. משטחים
    מעודנים (`--panel` מתרומם מ-`--bg` דרך ניגודיות משטח, לא רק צל) + `--border-strong`
    למצב פעיל/פוקוס. שינוי הטוקנים מתפשט אוטומטית לכל הרכיבים.
  - **סקאלת כפתורים וגופנים מהודקת**: כפתורי-הממשק ירשו 1rem מהגוף — גדול מדי;
    הבסיס ירד ל-`.9rem` עם `padding`/`min-height` קומפקטיים. **כפתורי בקרת ה-SDR
    ("⏹ עצור / חזור לקול" + "🎧 מגדל · 134.600") היו מנופחים ונשברו ל-2–3 שורות** —
    כעת גופן `.82rem`, שורה אחת (גם ב-360px); תווית המגדל קוצרה (הוסרו הסוגריים).
    צ'יפי הבנקים/סינון קלים ומעודנים יותר (משקל 500, `.78`/`.74rem`).
  - **סמל-מותג בכותרת**: האייקון ✈️ בתג מרובע מעוגן בגוון המותג; סימון הגרסה עדין יותר.
  - **מוקד ה-hero**: מספר התדר עם tracking מהודק וגרדיאנט מתכתי; רקע-דף עם הבהוב עליון
    עדין בגוון המותג + החלקת גופנים (`antialiased`) וקצב-שורה קריא.
  - **מפת המסלולים (ADS-B) מעוגנת** במשטח פנימי עדין במקום "לרחף" בחלל ריק.
  - **גרפי sparkline** (SNR + קצב הודעות) קיבלו מילוי-שטח מדורג → נקראים כמגמה מכוונת
    גם כשהם כמעט-שטוחים, במקום "מקף" תלוש.
- **תיקוני פרופורציה בתצוגות הדאטה (ACARS/VDL2)**:
  - שורת הסטטיסטיקה עברה ל-**grid של 5 עמודות שוות** → הפריט החמישי (`🛰 ADS‑B`) כבר לא
    "מתייתם" לשורה נפרדת ברוחב מלא.
  - **מזהים קבועים (זנב/טיסה/זמן) לא נשברים לעולם לאמצע שורה** ב-roster ובכרטיסי ההודעות
    (`white-space: nowrap` + `flex: none`); רק שם-הטיסה מתכווץ עם ellipsis. מקף לא-שובר
    בתווית `ADS‑B`.
  - **בורר המצבים המגזרי** לא "קופץ" ב-2px כשמופיע גבול פעיל (border שקוף בכל הכפתורים).

### נוסף
- **ליטוש UI מקיף — "שייראה כמו אפליקציה"** (סקירת עיצוב מלאה של שלוש התצוגות):
  - **נגן שמע מותאם:** כפתור ▶ עגול גדול (טבעת דופקת בזמן נגינה) + סליידר עוצמה
    (נשמר ב-localStorage; מוסתר ב-iOS שמתעלם מ-volume תוכנתי) במקום ה-`<audio controls>`
    הנייטיבי — עיצוב אחיד בכל דפדפן. לחיצה על ▶ תמיד מסנכרנת ל-live.
  - **סרגל המצבים (📻/📡/🛰️/⏻) דביק לראש המסך** עם blur — מחליפים מצב בלי לגלול
    חזרה גם בעומק פיד ה-ACARS/VDL2.
  - **אנימציית כניסה לכרטיסים חדשים בפיד** (מסומנים לפי id — רינדור מסינון/חיפוש לא
    מהבהב), ו-**badge ‏"↻ שודר שוב"** לרטרנסמיטים — ה-CSS ‏(`.amsg-retx`) היה קיים אך
    מעולם לא רונדר.
  - **זמנים יחסיים** ("לפני 2 דק׳", tooltip עם הזמן המלא) בפיד וב-roster בשתי תצוגות
    הדאטה; מתרעננים כל 30ש' דרך ה-stale timer הקיים.
  - **מפת הדאטה נמוכה יותר במובייל** (190px) — הפיד נכנס מעל קו המסך; בדסקטופ נשארת
    גדולה לצד הפיד.
  - **לוח ה-ATIS מודגש בענבר**, empty states עם אייקון (📡/🛰️), ומטרות מגע ≥36px
    לצ'יפים ולבנקים.
  - **נגישות:** `:focus-visible` לניווט מקלדת, `prefers-reduced-motion` לכל האנימציות
    הדופקות, `dir="auto"` בשדות החיפוש (מקלידים בעיקר זנבות/labels לטיניים).
  - תוקן `var(--fg)` שלא היה מוגדר (⏻ ובנקי תדרים) → `var(--text)`.
  - **רינדור אינקרמנטלי לפיד ה-ACARS/VDL2**: כל poll (3s) בנה מחדש עד 200 DOM nodes
    מורכבים גם כשרוב ההודעות לא השתנו. `reconcileFeed` (משותף לשתי התצוגות) משווה
    "חתימת תוכן" פר-הודעה מול מטמון פר-מפתח יציב (id ההודעה / מפתח block/רטרנסמיט
    ל-multi-block ולספיגת רטרנסמיט) — זהה => משתמש חוזר ב-node הקיים (רק מתעדכן הזמן
    היחסי), שונה/חדש => נבנה מחדש. אומת ב-Playwright: poll שקט משמר את זהות ה-DOM
    nodes של כל הכרטיסים; הודעה חדשה מוסיפה כרטיס אחד בלבד, השאר לא נוגעים.
  - **sparkline לקצב הודעות** (`📈` מתחת לסטטיסטיקה, כמו גרף ה-SNR בתצוגת הקול):
    מגמת 2 הדקות האחרונות, מתעדכן כל poll.
  - **שלד טעינה (skeleton)**: כרטיסי הפריסטים מוצגים כפלייסהולדר מרצד עד ש-`/api/state`
    חוזר (מוחלף אוטומטית ע"י `renderPresets()` — בלי לוגיקת הצג/הסתר נפרדת), ותצוגת
    התדר/סטטוס דוהה בעדינות במקום "—"/"טוען…" יבשים.

- **מצב VDL2 🛰️ — כרטיסיה שלישית לניתוח VHF Data Link Mode 2**: מתג המצב הופך
  תלת-כיווני (📻 קול / 📡 ACARS / 🛰️ VDL2), ו-`app_mode` הופך `voice/acars/vdl2/off`.
  VDL2 (D8PSK, 31.5kbps, ה-CSC העולמי 136.975MHz + 4 ערוצי אירופה) מפוענח ע"י
  **dumpvdl2 v2.6.0** (הגרסה הראשונה עם תמיכת RSP1B), דרך SoapySDR על אותו RSP1B
  בהחלפה (SDR אחד => צרכן אחד; `Conflicts` מול קול ו-ACARS גם יחד). הפלט זורם כ-JSON
  ב-UDP (פורט 5557) ל-listener חדש, כמו acarsdec.
  - **סוגר את המעגל מול ממצא 1.10.0**: שם נקבע ש-CPDLC *אינו* מופיע על ACARS VHF
    131.x כי חברות התעופה מריצות אותו על VDL2 — עכשיו הוא נקלט ומוצג.
  - **ACARS-over-VDL2 בטיפול מלא**: הודעות ה-ACARS שנישאות על AVLC עוברות דרך אותו
    `_normalize_acars` => *כל* הפרסרים הקיימים (ATIS/OOOI/PDC/loadsheet/label 15/16/1L/
    H1/ARINC-622) והקטגוריות חלים בחינם, בלי כפילות קוד.
  - **תוכן שאינו ACARS (CPDLC/ADS-C/XID) ככרטיס גנרי** עם תקציר libacars (בלי פרסרים
    ייעודיים חדשים בשלב זה). זהות המטוס נגזרת מכתובת ה-ICAO של שכבת ה-AVLC (שדה `icao`
    חדש) גם כשאין רישום, וכיוון ההודעה (uplink/downlink) נקבע מבנית מסוג כתובת ה-AVLC.
  - **תצוגה, היסטוריה וייצוא נפרדים**: כרטיסיית VDL2 עם פיד/roster/מפה/סטטיסטיקה/סינון
    (שימוש חוזר במחלקות ה-CSS של ACARS, אפס CSS חדש), היסטוריה ב-`vdl2.jsonl` (שורדת
    restart, היום-בלבד בטעינה), ייצוא ב-`/api/vdl2/export` (CSV עם BOM / JSON, עמודת icao).
  - **מסלול פריסה**: `install.sh` בונה את dumpvdl2 נעוץ ל-`v2.6.0` (חתימת בנייה,
    בנייה-רק-כשצריך, כמו rtl_airband), sudoers מקבל 2 שורות ל-restart/stop של השירות,
    ו-`airam-vdl2.service` מותקן אך *לא* enabled (מופעל לפי המצב ב-UI). libacars שודרג
    לשער `≥2.1.0` (דרישת dumpvdl2), ונוסף `libglib2.0-dev`.
### תוקן
- **מונה המטוסים המזויפים (`gps.spoofed_count` ב-`/api/airspace`) חושב שגוי:** בלוק
  ה-snapshot פר-מטוס (היתוך ACARS↔ADS-B) דרס את המשתנה `spoofed` המשמש כמונה —
  כל מטוס עם רישום איפס את הספירה (ואף הפך אותה ל-bool). שם משתנה נפרד + assertion
  רגרסיה ב-`adsb.py --selftest`.
- **‏`/.POS/` קיבל דקות לא-חוקיות (60–99):** בניגוד ל-label 15 ולפורמט הטקסט החופשי,
  ה-regex לא אכף דקות 00–59 — ספרת דקות שהתהפכה בפריים פגום (הפורמט נחלץ בכוונה גם
  עם error) הזיזה מטוס עד ~1.6° על המפה בשקט. עכשיו נדחה, כמו `_L15_RE`.
- **media advisory (SA) קיבל שעה 24–29** — נדחית (`[01]\d|2[0-3]`).
- **תיקוני סקירת קוד למצב VDL2** (8 ממצאים מסקירה עצמאית): מיזוג זהות מטוס בין
  הודעות tail-מבוססות (ACARS-over-VDL2) לבין icao-בלבד (CPDLC/XID) — מנע כפילות
  ב-roster/מפה; CPDLC כבר לא מקבל `pos_src="adsc"` מנ"צ מוטבע ב-clearance (רק
  ADS-C אמיתי מייצר מיקום); `install.sh` לא הורס checkout קיים של libacars לפני
  clone (מנע כשל התקנה offline); לוג תקופתי לפריימי VDL2 לא-מזוהים (עיוורון
  תפעולי אם dumpvdl2 ישנה סכמה); `api_state` בודק rtl_airband ראשון (חוסך 2
  קריאות `systemctl` מיותרות במצב הנפוץ); הגנת TDZ אמיתית סביב המשתנה `vdl2`
  ב-UI; איחוד `_append/_trim` ל-jsonl גנריים; `VDL2_EXPORT_COLS` כרשימה מפורשת.
- **4 פרסרי ACARS חדשים, כולם מבוססים על קליטה אמיתית מהשטח** (131.725/131.825MHz,
  LLBG): במקום להשקיע ב-CPDLC (נבדק ונמצא **שאין ממנו תעבורה בפועל** בקליטה שנבדקה —
  חברות התעופה כאן מריצות אותו כנראה על VDL2/לוויין, לא VHF 131.x) — פוענחו ארבעה
  labels אחרים שכן הופיעו בשפע:
  - **C1 — Loadsheet אלקטרוני**: `LOADSHEET FINAL` מפיק עכשיו `ZFW/TOW/TOF` (משקלי
    המראה) ומספר נוסעים/צוות. ההודעה מגיעה בבלוקים נפרדים (multi-block); כל בלוק
    מציג מה שהוא נושא.
  - **Label A3 — PDC (Pre-Departure Clearance)**: אישור טרום-המראה מלא בטקסט —
    יעד, מסלול המראה, SID, סקוואק, תדר הבא, גובה טיפוס ראשוני. זה ה-ACARS clearance
    האמיתי שכן קיים בשטח (בניגוד ל-CPDLC הבינארי).
  - **Label 16 — דיווח מיקום עשרוני**: פורמט לא-מתועד שזוהה מקליטה אמיתית
    (`WPT ,N dd.ddd,E ddd.ddd,ALT,...`). קודם הוצג `lat=null`; עכשיו מזין את המפה
    (`pos_src="label16"`, מגודר ע"י `error` בדומה ל-heuristic הטקסטואלי).
  - **Label 1L — דוח ניווט/דלק**: `N.../E.../UTC.../FOB.../ALT.../CAS.../ETA...` —
    נ"צ + דלק על הסיפון + גובה/מהירות/ETA (`pos_src="nav-fuel"`, פורמט מבני-מספיק
    לחילוץ גם עם `error>0`, כמו `/.POS/`).
### תוקן
- **תג-סוג פנימי של libacars מוצג כאילו הוא תוכן**: הודעות ADS-C אמיתיות הציגו
  `decoded: "adsc_msg"` — זהו מזהה-סוג פנימי (snake_case) של libacars, לא טקסט. 
  `_libacars_decode` מסנן עכשיו ערכים בצורת `snake_case` נקיה (תגי-סוג), בעוד תוכן
  אמיתי (גם מילה בודדת כמו `WILCO`) ממשיך לעבור.
### בדיקות
- **35 טסטים חדשים ל-VDL2** ב-`tests/test_vdl2.py` (וקטורים בנויים לפי סכמת ה-JSON
  של dumpvdl2 v2.6.0 שאומתה מהמקור): נרמול מסלול-A (ACARS-over-VDL2 עם מיקום/ACK ריק/
  ARINC-622 מקונן) ומסלול-B (CPDLC/ADS-C/XID/פריים ריק), כיוון מבני שדורס heuristic,
  המרת Hz ב-env, ולידציית חלון, roundtrip UDP אמיתי על 5557, כל מעברי המצב (כניסה/
  כישלון+rollback/דחיית חלון רחב/עצירה מ-voice/tune/acars/standby-שלושה), state/health
  מרובעים, התמדה וייצוא. עדכון `test_api_mode_off_stops_both` → standby עוצר שלושה צרכנים.
- 17 טסטים חדשים, רובם עם וקטורים מדויקים מקליטה אמיתית (לא סינתטיים): כל 4
  הפרסרים החדשים + מקרי דחייה (וריאנט קצר/פורמט לא תקין) + regression לתיקון ה-`adsc_msg`.
  כל 583 ההודעות מהקליטה שנבדקה עברו דרך `_normalize_acars` ללא חריגה אחת.

## [1.9.0] - 2026-07-02
### נוסף
- **היתוך ADS-B ↔ ACARS 🛰**: נתוני ה-ADS-B שכבר נמשכים (adsb.lol, פעם בדקה, עבור
  זיהוי המסלול הפעיל) נשמרים עכשיו כ-snapshot פר-מטוס ומוצלבים עם זנבות ה-ACARS
  (נרמול רישום: `.4X-EHD` של acarsdec ↔ `4X-EHD` של ADS-B). בממשק: badge **🛰**
  "משדר כעת" ברשימת המטוסים, **סוג מטוס סמכותי** (גובר על ה-heuristic מהטקסט),
  פאנל פירוט עם callsign/גובה/מהירות/כיוון חיים, **שובל ADS-B חי** (מקווקו) במפה
  למטוס הנבחר, ותא סטטיסטיקה חדש. עמידות שיבוש GPS: ‏`nic<2` ⇒ הנ"צ המזויף מדוכא
  ומוצגת התראת "GPS משובש ⚠" (גובה/מהירות/כיוון שורדים). המימוש: `norm_reg` +
  `aircraft_snapshot(regs)` ב-`adsb.py` (גיזום זיכרון אחרי 10 דק'), שדה `adsb` חדש
  בתשובת `GET /api/acars` (אין אינטרנט ⇒ `{}` — הרדיו לא תלוי ברשת).
- **חבילת פענוח ACARS עמוק** — 6 פרסרים מבניים חדשים (dispatch לפי label, בלי
  false positives בין labels):
  - **H1 sub-labels** (`#DF`/`#M1-3`/`#CF`/`#EC`/`#T0-8`...): זיהוי מקור ההודעה
    (FMC / מקליט נתונים / תחזוקה / מנוע / מסוף תא) + "דיווח מיקום" כש-POS צמוד להדר.
  - **`/FPN/` בתוך H1**: פענוח תוכנית טיסה — יציאה→יעד (`:DA:`/`:AA:`) ורשימת
    waypoints מ-`:F:` (עד 8, נ"צ צמוד נחתך).
  - **SA (media advisory)**: איזה קישור נוצר/אבד (VHF/SATCOM/HF...), שעה, ומדיות זמינות.
  - **Label 15 (דיווח מיקום קלאסי)**: נ"צ מ-`(2N…E…` — פורמט מעוגן-מבני שנחלץ גם עם
    `error>0` (כמו `/.POS/`), עם `pos_src` חדש `"label15"` (מזין את המפה).
  - **SQ**: מפוענח כ-squitter תחנת קרקע (IATA+ICAO+תדר) — בלי חילוץ נ"צ (נ"צ התחנה,
    לקח 1.7.1) + **תיקון כיוון ל-uplink** (בעבר סומן downlink בטעות).
  - **`:;` (autotune)** ו-**54 (voice go-ahead)**: תיאור קריא + כיוון uplink.
### תוקן
- **מרוץ נתונים ב-`/api/acars`**: עדכון `retry_count` ע"י ה-listener רץ עכשיו תחת
  `_acars_lock`, וה-route מסדרל **עותקים** של ההודעות — מוטציה באמצע איטרציית ה-JSON
  encoder הייתה עלולה להפיל את הפולינג ב-`RuntimeError` (תשובת 500).
- **בקשות `pollAcars` חופפות ב-UI**: נוסף מנעול in-flight (כמו `pollMetrics`) — שתי
  בקשות מקבילות עם אותו `since` הכפילו כרטיסים, נקודות שובל ומונים.
- **התאוששות אחרי restart של sdrplay**: ‏`PartOf=sdrplay.service` לשני צרכני ה-SDR —
  עצירה שמתפשטת דרך `Requires` היא dependency-stop "נקייה" ש-`Restart=always` לא
  מתאושש ממנה; ‏`PartOf` מחזיר (try-restart) את הצרכן הפעיל בלבד ⇒ התחנה לא נשארת שקטה.
- **כלל udev בעט ממצב ACARS/standby**: חיבור USB הריץ `restart rtl_airband` ישירות,
  שדרך `Conflicts` עצר את acarsdec (או הוציא מ-standby). עכשיו הכלל מריץ רק
  `restart sdrplay.service`, וההתאוששות מגיעה מ-`PartOf` לפי המצב בפועל.
### בדיקות
- 26 טסטים חדשים: עותקים ב-`/api/acars`, כל פרסר חדש (וקטורים אמיתיים + דחיית זבל),
  ‏`test_adsb_enrich.py` (נרמול, דיכוי spoofed, גיזום, סינון, אינטגרציית `/api/acars`)
  והרחבת `adsb.py --selftest`. בוצע גם אימות end-to-end בדפדפן (Chromium מול השרת).

## [1.8.0] - 2026-06-28
### נוסף
- **תצוגת ACARS "היום בלבד"**: סשן חדש מציג רק את ההודעות שנקלטו *היום* (שעון ה-Pi)
  במקום להציף את הפיד בתעבורת ימים קודמים. ההיסטוריה המלאה נשמרת בדיסק
  (`acars.jsonl`) וזמינה בייצוא CSV/JSON וגם דרך `GET /api/acars?all=1`.
  המימוש: רצפת-יום ב-`_load_acars_history` (טעינה לזיכרון) ובמסנן `/api/acars`.
- **כיבוי/הפעלת המקלט מהממשק (Standby)**: כפתור ⏻ במתג המצב עוצר את *שני* צרכני
  ה-SDR (rtl_airband + acarsdec) ומשחרר את ה-RSP1B ליישום SDR אחר — בעוד הדף נשאר
  פעיל ומאפשר להדליק שוב בכל רגע. `sdrplay.service` נשאר חי (ה-API daemon מתווך
  לאפליקציה הבאה). מצב `app_mode` שלישי חדש: `off`. אינו שורד reboot (rtl_airband
  עולה מחדש ⇐ ברירת מחדל בטוחה). ה-sudoers ללא שינוי (stop כבר היה מותר).
- **בנקי תדרי ACARS**: בורר בנקים בכותרת ACARS. שני בנקים מובנים שכל אחד נכנס
  בחלון דגימה אחד: **"אירופה + עולמי (131)"** (ברירת מחדל, 130.450–131.850, כולל
  131.550 שעליו מופיעים גם מטוסי תדלוק/צבא אמריקאים) ו-**"אזור 136"** (136.700–136.975).
  צביר 131.x ו-136.x רחוקים ~5MHz ⇒ לעולם לא בחלון אחד ⇒ בנקים נפרדים. ולידציה
  חדשה (`_acars_window_error`): עד 8 ערוצים ו-span ≤ 1.9MHz, אחרת הבקשה נדחית.
### שונה
- ברירת המחדל של תדרי ACARS הורחבה מ-4 ל-7 ערוצים (בנק "אירופה + עולמי").
### בדיקות
- 12 טסטים חדשים: ולידציית חלון/בנקים, טעינת "היום בלבד" + מסנן `/api/acars` + `all=1`,
  שמירת היסטוריה מלאה בייצוא, ומחזור standby (`off` עוצר את שניהם, state/health,
  חזרה לקול). שני טסטים קיימים עודכנו ל-timestamps של היום.

## [1.7.3] - 2026-06-28
### נוסף
- **dedup ACARS retries**: כשמטוס לא מקבל ACK הוא שולח את אותה הודעה שוב ושוב
  (נצפה: APU fault של OO-ACF נשלח 7 פעמים). כעת `_acars_listener` מזהה retry
  לפי `(tail, label, text[:80])` בחלון 90 שניות — במקום כרטיס חדש, `retry_count`
  מצטבר על הכרטיס המקורי. JSONL נשמר נקי מחזרות; ה-UI מציג אירוע אחד.
- **Label `WX` (בקשת מזג אוויר)**: label שנקלט בשטח מ-9H-CAC (METAR ל-LGRP/LYBE/LIMC/LFPG)
  מוצג עכשיו עם תיאור קריא ו-`decoded` אוטומטי:
  - שדה בודד → `"WX: LCLK"` (בקשת מזג אוויר ליעד/חלופה יחידה)
  - שני שדות+ → `"ALTERNATE: LGRP · LYBE · LIMC · LFPG"` (alternate planning פעיל)
  - LLBG מוסננת (שדה הבית, אינה "alternate"). כיוון: `downlink`.
### בדיקות
- 7 טסטים חדשים: `_parse_wx_alternates` (multi/single/LLBG/None), WX ב-normalize,
  dedup key logic, ו-retry_count update.

## [1.7.2] - 2026-06-28
### נוסף
- **Label `3L` (ULD/מטען)**: label שהוצג בקליטה אמיתית מ-D-AIDA מסווג כעת כ-"נתוני ULD/מטען"
  בקבוצה `tech` (אפור), כיוון `downlink`. קודם הופיע כ-"Label 3L".
- **Label `5V` (VHF link mgmt)** ו-**`A4` (FSM)**: שני labels נוספים שנקלטו בשטח זוהו
  ומוצגים עם תיאור קריא ב-UI במקום "Label X".
- **פרסר `/.POS/` (REQPOS response)**: הודעות תגובה לבקשת מיקום מהקרקע מחלצות עכשיו
  נ"צ + waypoint הבא + ETA (שדה `decoded`). הפורמט הוא מבני לחלוטין ולכן **אמין גם עם
  `error>0`** (בניגוד לחילוץ heuristic מטקסט חופשי). `pos_src` = `"pos-report"`.
  דוגמה: `/.POS/TS104451,260626N32006E034539,,104451,1,VELOX,110451,,P31,,147,F566`
  → lat=32.01, lon=34.90, decoded="WPT VELOX · ETA 11:04z · F566".
### בדיקות
- 5 טסטים חדשים: label 3L/5V/A4, `_parse_pos_report` (basic + error bypass + reject).

## [1.7.1] - 2026-06-28
### תוקן
- **מיקומי login (LLBG) שגויים על המפה/בכרטיסים**: הודעות login/SQ נושאות את נ"צ
  ה*שדה* (`...LLBG03200N03452E...` ≈ נתב"ג, reference משותף לכל מטוס שמתחבר) ולא את
  מיקום המטוס. החילוץ הקודם הדביק 📍 מטעה על כל הודעת login (בקליטה לדוגמה: 73 כאלה).
  ה-regex `_TEXT_POS_LLBG_RE` הוסר — נ"צ אמיתי מגיע מדיווחי position (compact/decimal)
  ומ-ADS-C, לא מ-frame של login.
- **חילוץ מיקום מ-frame משובש**: הודעות עם `error > 0` (acarsdec תיקן/החמיץ ביטים)
  כבר לא מפיקות מיקום מ-heuristic הטקסט — ספרה שהתהפכה בקואורדינטה הייתה ממקמת מטוס
  בנקודה שגויה. ADS-C (libacars, מוגן-CRC) ממשיך להיחלץ גם עם `error`.

### נוסף
- בדיקות regression מתוך קליטת ACARS אמיתית: login אינו מפיק מיקום, ו-frame עם
  `error>0` מדלג על חילוץ מיקום טקסטואלי.

## [1.7.0] - 2026-06-26
### תוקן
- **Bug #1 — הודעות multi-block מוצגות כ-N כרטיסים נפרדים**: הודעות שה-msgno שלהן הוא
  בפורמט `[A-Z]\d\d[A-Z]` (כגון D57A, D57B … D57K) מקובצות כעת בתצוגה לכרטיס אחד —
  הגרסה האחרונה (הכי שלמה) עם badge `×N`. ה-grouping הוא VIEW-only; כל ההודעות נשמרות.
- **Bug #2 — שדה `error` לא מוצג**: הודעות עם `error > 0` מציגות כעת badge `CRC ⚠` /
  `link ⚠` / `decode ⚠` באדום בשורת top הכרטיס — מאפשר לזהות פרסורים חלקיים.
- **Bug #3 — Label `80` מסווג כ-"text" במקום "oooi"**: תוקן — label 80 (OFFRP/INRP)
  מסווג עכשיו לקבוצה `oooi` (כתום) ומכוון `downlink`. נוספו גם:
  - `A9` → קבוצה `comm`, כיוון `uplink` (ATIS מהקרקע).
  - `C1` → קבוצה `text`, כיוון `downlink` (הודעת חברה מהמטוס).
  - `Q0` → כיוון `downlink` (link test ממטוס, לא עמום).

### נוסף
- **ATIS parsing (A9) ולוח sticky**: הודעות A9 מפוענחות אוטומטית — מסלול פעיל, כיוון
  רוח ומהירות, QNH — ומוצגות בלוח נייח מעל הפיד שמתעדכן לכל הודעת A9 חדשה.
- **ספיגת רטרנסמיט**: הודעות זהות (אותו tail+label+text[:50]) שהגיעו בתוך 30 שניות
  זו מזו מוצגות פעם אחת — נשמרת זו עם קוד שגיאה נמוך יותר (VIEW-only).
- **תצוגה מובנית ל-Label 80 (OFFRP/INRP)**: זמני OUT/OFF/ON/IN מחולצים מהטקסט
  ומוצגים בשדה `decoded` הבולט (כגון "OUT 14:20 · OFF 14:32").
- **חילוץ סוג מטוס (actype)**: `_normalize_acars` מחלץ best-effort את סוג המטוס
  מהודעות H1/C1 (תבניות B7xx, A3xx, E1xx וכו') ומוסיף שדה `actype`. מוצג ברשימת
  המטוסים (roster) ובייצוא CSV.
- **צביעת רמת אות**: ה-dBFS בשורת meta של כל כרטיס מקבל צבע — ירוק (>−30), צהוב
  (−30 עד −45), אדום (<−45) — לזיהוי מהיר של קליטה חלשה.
- **7 בדיקות אוטומטיות חדשות**: מכסות את כל שינויי ה-backend (labels 80/A9/C1, ATIS
  parsing, OOOI-80 parsing, actype extraction, עמודת actype בייצוא).

## [1.6.1] - 2026-06-26
### תוקן
- **חילוץ נ"צ מטקסט ACARS — שני פורמטים נוספים**: ניתוח של הודעות אמיתיות גילה שהרג'קס
  הקיים מפספס 0 מתוך 15 הודעות עם מיקום. שני תיקונים ב-`_text_latlon`:
  - **פסיק בין lat ל-lon**: פורמט `N3203.7,E03455.7` (H1 של B737, נפוץ בהודעות נחיתה/מסלול)
    לא נחלץ כי הרג'קס ציפה לרווח בלבד; עכשיו `[,\s]?` מאפשר גם פסיק.
  - **ספרה עשרונית ללא נקודה** (`DDMMf/DDDMMf`): פורמט קומפקטי `N32042E034560`
    (= N 32°04.2′ E 034°56.0′) שבו ספרת העשרון צמודה לדקות ללא `.` — טופל עם רג'קס נפרד
    (`_TEXT_POS_COMPACT_RE`). נתמך גם `POSN32010E034540` ו-`FPON39428E038506`.
  - תוצאה: 5 מטוסים עם נ"צ לעומת 0 לפני הפתרון (LY-LOC, 4X-EHD, 4L-GIT, OE-LRI, N375WB).
  - 2 בדיקות חדשות נוספו (`test_text_latlon_comma_separated`, `test_text_latlon_compact_no_decimal`).

## [1.6.0] - 2026-06-26
### נוסף
- **ACARS — הנגשה: מפה לפי בחירה, רשימת מטוסים, ופאנל פירוט ✈️🗺️**: שיפור חוויית
  השימוש סביב המפה והפיד.
  - **מפה לפי בחירה**: המפה נקייה כברירת מחדל (במקום להציג את כל המטוסים בכל רגע).
    בחירת **מטוס** (מהרשימה / קליק על זנב / chip 📍 בהודעה) מציגה **רק אותו** —
    marker + שובל מסלול — וממקדת אליו; ניקוי הבחירה מרוקן את המפה. נטל ה-situational
    awareness עובר ל**רשימת המטוסים**.
  - **רשימת מטוסים (roster)**: פאנל לבחירה לפי זנב — מס' הודעות, חיווי `📍` למי שיש לו
    מיקום, כיוון אחרון וזמן "נראה לאחרונה"; ממוין לפי הפעילים. מתקפל במובייל.
  - **פאנל פירוט למטוס** (מתחת למפה): מיקום אחרון + מקור, label אחרון, סה"כ הודעות,
    פילוח כיוון, חלון זמן, ומיני-היסטוריה של ההודעות האחרונות.
  - **כיוון uplink/downlink**: כל הודעה מסווגת (best-effort) ל-↑uplink (קרקע→מטוס) /
    ↓downlink (מטוס→קרקע) לפי label ו-header ניתוב; מוצג כ-badge בכרטיס/רשימה ובייצוא
    (עמודת `dir`).
  - **דהיית "יצא מטווח" (stale)**: מטוס שלא נשמע מעליו מעל 10 דק' מעומעם ברשימה ובמפה
    (ב-ACARS פסיבי מטוסים "נעלמים" כשהם יוצאים מטווח VHF).

## [1.5.0] - 2026-06-26
### נוסף
- **ACARS — שובל מסלול במפה + drill-down למטוס ✈️**: המפה מציירת **שובל** (polyline)
  המחבר מיקומים עוקבים של אותו זנב — רואים את נתיב הטיסה, לא רק את הנקודה האחרונה
  (צבע יציב לכל מטוס; עד 60 נקודות). **לחיצה על זנב/טיסה** (בכרטיס או ב-popup של המפה)
  מסננת את הפיד **למטוס הזה בלבד** וממקדת את המפה על המסלול שלו; צ'יפ "מטוס: … ✕" מנקה.
  - הקשחה: ה-popup של ה-marker נבנה כ-DOM (text nodes) במקום HTML גולמי — טקסט ACARS
    (קלט חיצוני) כבר לא יכול להזריק HTML.

## [1.4.0] - 2026-06-25
### נוסף
- **ACARS — שמירה, ייצוא ותצוגה ויזואלית 🗺️**: הודעות ה-ACARS עכשיו **נשמרות ושורדות
  אתחול** (`/var/lib/airam/acars.jsonl`, ממוין לפי זמן; זנב נטען לזיכרון בעלייה), עם
  **ייצוא** מסודר לניתוח (`GET /api/acars/export?format=csv|json` — CSV עם BOM לעברית
  ב-Excel).
  - **תצוגה אחידה לכל הפורמטים**: כל הודעה מקבלת **קטגוריה קריאה** (מילון labels + פענוח
    ARINC-622/CPDLC/ADS-C דרך `libacars`) ו-**badge צבעוני** לפי קבוצה (מיקום/ATC/OOOI/
    טכני/תקשורת/טקסט). פענוח (CPDLC וכו') מוצג בולט, והגולמי מתקפל.
  - **מפת Leaflet** (vendored מקומית, אריחי OpenStreetMap; נפילה חיננית לרשימת מיקומים
    אם אין אינטרנט) — מטוסים עם מיקום מ-ACARS (ADS-C/CPDLC, וגם heuristic לטקסט). chip
    `📍` בכרטיס ממקד את המפה.
  - **פוליש UI**: חיפוש (זנב/טיסה/label/טקסט) + צ'יפים לסינון קטגוריה, שורת סטטיסטיקה
    (הודעות · הודעות/דק׳ · מטוסים · עם מיקום), שימור מיקום גלילה, ומצבים ריקים ברורים.

## [1.3.0] - 2026-06-25
### נוסף
- **מצב משולב — קול + ACARS 📡**: מתג מצב בראש הממשק (📻 קול / 📡 ACARS). מצב
  ACARS מריץ `acarsdec` על אותו RSP1B (בהחלפה עם `rtl_airband` — מקלט אחד, לכן
  לא בו-זמני) ומציג הודעות ACARS מפוענחות חיות בדף (זמן, תדר, זנב, טיסה, label,
  טקסט). כפתור "🎧 האזן למגדל" חוזר לקול ומכוונן ל-134.6.
  - שירות `airam-acars` (לא enabled; מופעל לפי המצב) עם `Conflicts=rtl_airband`
    => לעולם לא שני צרכני-SDR יחד. `acarsdec -j` שולח JSON ב-UDP ל-`airam-web`
    (listener + ring-buffer), והדף מושך מ-`/api/acars`.
  - הגדרות ב-`/etc/airam/acars.env` (פורמט EnvironmentFile של systemd — מנותח
    בבטחה, בלי הסלמת airam→root). סט אירופה/ישראל: 131.525/131.550/131.725/131.825.
  - `install.sh` בונה `libacars` + `acarsdec` (`-Dsoapy=ON`); sudoers הורחב
    לפעולות ה-systemctl המדויקות בלבד (stop rtl_airband, restart/stop airam-acars).
  - נתיבים חדשים: `POST /api/mode` (מעבר קול↔ACARS, מוגן Origin+PIN) ו-`GET /api/acars`.

## [1.2.0] - 2026-06-19
### נוסף
- **תמיכת HTTPS + PWA מלא**: כשהדף מוגש ב-HTTPS (למשל מאחורי `tailscale serve`),
  נגן הדפדפן עובר אוטומטית ל-proxy same-origin (`/stream`) שמגיש את סטרים
  ה-Icecast דרך שרת הווב — נמנע חסימת mixed-content. ב-HTTP/LAN הגישה ל-Icecast
  נשארת ישירה (latency נמוך). שרת הווב רץ כעת ב-`threaded` (סטרים ארוך-טווח לא
  חוסם בקשות). תיעוד `tailscale serve` ל-HTTPS מהימן והתקנת אפליקציה בטלפון.

## [1.1.0] - 2026-06-19
### נוסף
- **תמלול ATC (אופציונלי)**: `whisper.cpp` מקומי (מודל `base.en`) מתמלל כל שידור
  מוקלט; הטקסט מוצג מתחת לשידור ביומן וניתן לחיפוש (תיבת חיפוש ביומן). רץ ב-thread
  רקע נפרד וכותב קובץ-צד `<file>.mp3.txt`. **כבוי כברירת מחדל** — הפעלה:
  `INSTALL_WHISPER=1 sudo ./install.sh` (מתקין `ffmpeg`, בונה `whisper.cpp`, מוריד
  מודל, ומפעיל `AIRAM_TRANSCRIBE=1`). התקנות קיימות לא מושפעות.
- **נגן חיצוני**: כפתור "🎧 נגן חיצוני" ונתיב `/live.m3u` לפתיחת השידור בנגן שמע
  חיצוני (ניגון ברקע חסין); באנדרואיד דרך Android Intent (פותח VLC).
- **פקדי מסך נעילה**: מעבר בין פריסטים (previous/next) דרך Media Session.

## [1.0.0] - 2026-06-19
גרסה ראשונה מתויגת — לוכדת את המצב היציב הקיים של המערכת.

### היכולות העיקריות
- **בורר תדרים וובי** (Flask) לבחירת תדר/פריסט מהטלפון; כתיבת `airband.conf` והפעלה
  מחדש מאומתת של `rtl_airband` עם רולבק.
- **השהיה נמוכה** (CBR 48kbps + Icecast burst=0) ו-**מדדי RF חיים** (SNR/signal/noise,
  רענון ~1s) לכיוון אנטנה.
- **עריכת פריסטים** מהממשק, **סקוולץ׳** (auto/open/manual), **רווח IFGR/RFGR** + חיווי עומס.
- **הקלטות ויומן שידורים**, **METAR** של נתב״ג, **מסלול נחיתה/המראה פעיל + חיווי שיבוש GPS**
  מ-ADS-B, ו-**חיווי הספקה** (vcgencmd).
- **PWA** (התקנה למסך-הבית) + שמע ברקע, ו-**`/api/health`** לאבחון.
- **חיזוק אבטחה**: שרת הווב רץ כמשתמש לא-root (`airam`) עם sudoers ממוקד, אימות PIN
  אופציונלי (`AIRAM_PIN`), והגנת Origin/CSRF.
- **בדיקות + CI** (pytest + `bash -n`).

[Unreleased]: https://github.com/Shahar373/AIR-AM/compare/v1.4.0...HEAD
[1.4.0]: https://github.com/Shahar373/AIR-AM/compare/v1.3.0...v1.4.0
[1.3.0]: https://github.com/Shahar373/AIR-AM/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/Shahar373/AIR-AM/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/Shahar373/AIR-AM/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/Shahar373/AIR-AM/releases/tag/v1.0.0
